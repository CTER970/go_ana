# go_ana 改造为个人围棋学习系统

## 基于当前 v4.38 代码的产品、数据、算法与实施方案

---

# 一、最终目标

本次改造不应该把 `go_ana` 继续发展成：

> 功能更多的 KataGo GUI。

而应该明确转向：

> **基于个人历史实战、能够发现重复错误、主动测试理解程度、持续追踪错误是否改掉的个人围棋学习系统。**

最终核心闭环：

**导入自己的棋谱**

↓

**KataGo建立客观事实**

↓

**识别最值得本人学习的局面**

↓

**先让本人重新思考**

↓

**评价新的选择是否合理**

↓

**解释错误的棋理与可迁移原则**

↓

**归入个人错误模型**

↓

**生成错题并间隔复习**

↓

**观察以后真实棋局是否再次犯错**

↓

**更新个人学习画像与下一阶段训练重点**

真正的产品核心不是“这一手AI下哪里”，而是：

> **这个棋手正在反复犯什么错误，他现在最值得修正什么，以及这个问题后来有没有真正消失。**

---

# 二、对当前仓库的总体评价

当前仓库已经远超“围棋AI GUI 原型”的阶段。

README 显示当前版本为 v4.38，已经拥有：

* KataGo 本地 Analysis Engine；
* SGF 导入、导出；
* 批量导入；
* 持久化分析队列；
* 每手目损；
* 问题手；
* 胜率/目差走势；
* 双分支深度对比；
* 三段式问题解释；
* 候选手分层；
* 阶段训练；
* 训练后评价；
* 跨棋局错题本；
* 间隔复习；
* 长期个人画像；
* 棋风画像；
* 成长路线；
* 高 visits 复核。

也就是说：

> **大约70%的基础设施已经存在。**

真正需要重新设计的是剩下最重要的30%：

### 1. 什么错误最值得学

### 2. 什么才算“答对”

### 3. 为什么错

### 4. 错误是否重复

### 5. 是否真正学会

### 6. 什么解释可信

### 7. 当前水平应该学什么

这七件事决定产品是不是一个真正的“学习系统”。

---

# 三、第一原则：不要推倒当前架构

当前已有非常有价值的纯逻辑模块。

建议保留并稳定：

* `board.py`
* `movetree.py`
* `katago_client.py`
* `analysis_guard.py`
* `analysis_queue.py`
* `project_store.py`
* `game_library.py`
* `sgf.py`
* `heatmap.py`
* `branch_comparison.py`
* `deep_verification.py`

当前架构已经把棋盘状态、KataGo IPC、棋谱、分析队列和多数学习逻辑拆开，这一点做得正确。

不要重新设计棋盘规则。

不要自己实现围棋AI。

不要更换KataGo。

不要第一阶段迁移Web。

不要第一阶段换掉Tkinter。

这些事情技术上很诱人，但对“帮助自己涨棋”的贡献接近零。

---

# 四、当前最大的工程问题：app.py 太大

当前 GitHub 页面显示：

> `app.py` 约 9,266 行，约 449KB。

而项目说明中的旧统计仍写着约2,311行，说明代码继续增长以后文档已经出现漂移。

这已经达到需要控制的程度。

但：

> **不要先进行一次巨型重构。**

正确做法是随着新学习模块开发，逐步把UI拆出去。

建议最终形成：

```text
analyzer/
    app.py

    core/
        board.py
        movetree.py

    engine/
        katago_client.py
        analysis_guard.py
        analysis_queue.py
        human_sl.py

    analysis/
        review.py
        move_quality.py
        branch_comparison.py
        learning_priority.py
        candidate_assessment.py
        complexity.py

    learning/
        learning_event.py
        taxonomy.py
        mistake_book.py
        mastery.py
        training_analysis.py
        learning_profile.py

    coaching/
        evidence_packet.py
        deterministic_explanation.py
        coach_provider.py
        coach_schema.py

    storage/
        game_library.py
        project_store.py
        learning_store.py
        migration.py

    ui/
        board_view.py
        review_view.py
        library_view.py
        quiz_view.py
        profile_view.py
        advanced_analysis_view.py
```

不是立即整体搬迁，而是：

> **新功能禁止继续堆入 app.py。**

---

# 五、当前最需要纠正的功能一：

# “当前棋力参考”并不是真正的棋力模型

现在的 `candidate_recommendation.py` 已经非常谨慎地承认：

> 当前没有 Human SL。

代码里的“当前棋力参考”实际上来自：

* 候选目损；
* PV长度；
* KataGo `prior`；
* 当前单局表现标签；
* 人工设置的容忍目数。

例如代码会根据“段”“级”“职业”等字符串选择不同目损容忍值，然后把小目损、短PV、高prior的候选标成“当前棋力参考”。代码也明确写明 `humanModel=False`，并强调 prior 不是人类落子概率。

这个设计作为临时启发式是诚实的。

但产品文案容易让用户误解。

## 第一阶段立即修改

把：

> 当前棋力参考

暂时改成：

> **易执行候选**

或：

> **实战稳健选择**

直到 Human SL 真正接入。

---

# 六、正式接入 KataGo Human SL

这是整个改造中非常重要的一步。

KataGo 官方 Analysis Engine 已经支持：

```text
-human-model <human-model>
```

然后通过：

```text
overrideSettings:
    humanSLProfile = rank_1d
```

并开启：

```text
includePolicy = true
```

即可得到 `humanPolicy` / `humanPrior`。

支持的 profile 包括 `rank_20k` 到 `rank_9d` 等不同水平。

当前 `katago_client.py` 的启动参数只有：

```text
katago analysis
-config ...
-model ...
```

并没有 `-human-model`。

因此第一处明确代码改动是：

## 修改 `katago_client.py`

构造函数增加：

```text
human_model_path=None
```

启动：

```text
katago analysis
-config analysis.cfg
-model main_model
-human-model human_model
```

如果未配置：

> 正常退回普通KataGo。

---

# 七、增加 human_sl.py

新增：

```text
human_sl.py
```

职责不要放进 UI。

负责：

### 输入

* 当前局面；
* 用户棋力；
* 用户执棋颜色；
* 想比较的更高棋力。

例如：

```text
current_profile = rank_1d
target_profile = rank_3d
```

### 输出

例如：

```text
HumanMoveProfile
    move
    current_rank_prior
    target_rank_prior
    delta
```

---

# 八、Human SL真正应该解决的问题

不要把 Human SL 主要用于：

> “模拟一个1D AI。”

对本产品更重要的是：

> **判断某种错误是否属于当前棋力特别值得解决的问题。**

KataGo官方文档自己提出：

可以比较：

> 当前等级玩家下某手的 Human Policy

与：

> 高若干级玩家下同一手的 Human Policy，

甚至提出是否可以按照“高4级玩家明显更少下这类错误”来给复盘问题排序。

这正好应该成为本产品的特色能力。

---

# 九、当前最需要重做的功能二：

# Top问题手不能继续只按目损排序

当前 `player_profile.py` 生成单局 Top Problem 时：

```text
problems.sort(key=score_loss, reverse=True)
```

然后取前 `top_problem_limit` 个。

`problem_drill.py` 也基本采用：

> 达到目损阈值 → 按目损降序 → 取前N个。

这是一个优秀“分析器”的逻辑。

但不是优秀“学习系统”的逻辑。

例如：

### 第83手

复杂对杀：

> 亏12目。

但是：

> 职业棋手都可能需要深算。

### 第51手

很简单：

> 亏4目。

但过去20盘已经出现9次相同“自动跟应”。

真正应该重点学习：

> 第51手。

---

# 十、新增核心模块 learning_priority.py

这是未来产品最核心的算法模块之一。

定义：

```text
LearningPriority
```

每个关键局面得到：

```text
severity
recurrence
level_gap
learnability
game_importance
mastery_penalty
diversity_penalty

final_score
```

---

# 十一、第一版 Learning Priority 公式

第一版不要假装这是机器学习模型。

使用可解释、版本化启发式：

```text
priority =
    0.35 × severity
  + 0.25 × recurrence
  + 0.15 × level_gap
  + 0.15 × learnability
  + 0.10 × game_importance
```

然后：

```text
× mastery_modifier
× diversity_modifier
```

所有分量：

> 归一化到0～1。

权重必须保存：

```text
priorityAlgorithmVersion = 1
```

以后才能比较算法变化。

---

# 十二、Severity：错误严重度

主要来自：

> Point Loss。

胜率损失只作为辅助。

例如：

```text
severity = min(score_loss / 8, 1)
```

不是数学真理。

只是初始版本。

后续通过实际复盘数据调整。

---

# 十三、Recurrence：重复程度

这是现项目最值得强化的数据。

如果过去30盘：

> “弱棋未安定时强行攻击”

出现：

```text
1次
```

与：

```text
9次
```

优先级完全不同。

第一版：

```text
0次     0
1次     0.25
2次     0.40
3-4次   0.65
5次+    1.0
```

以后再改成连续函数。

---

# 十四、Level Gap：水平差异

有 Human SL 后计算：

```text
HumanPrior(current rank, actual move)
HumanPrior(current rank + 2~4 ranks, actual move)
```

如果：

```text
1D经常下
3D已经明显少下
```

这是优质学习点。

如果：

```text
1D很少下
5D也很少下
```

可能只是一次随机失误。

如果：

```text
1D和5D都经常下
```

说明这可能不是一个适合用“水平差异”解释的问题。

---

# 十五、Learnability：可学习程度

这是当前系统还缺少的重要概念。

简单错误：

> 优先。

AI唯一神之一手：

> 降低。

第一版可以综合：

### 合理候选数量

例如：

```text
≤1.5目候选有4个
```

说明局面可能没有唯一答案。

### 候选评价分布

第一与第二差距很小：

> 更容易学习成原则。

第一比第二好很多：

> 可能是具体计算题。

### Human Policy分布

如果同水平玩家高度分散：

> 局面本身难。

如果绝大多数较强棋手集中于几个方案：

> 更适合作为训练点。

### KataGo scoreStdev

可以作为弱辅助。

KataGo官方提醒 `scoreStdev` 当前可能存在偏高，因此不能把它直接等同“局面复杂度”。

---

# 十六、Game Importance

保存对局情境：

```text
网络快棋
网络慢棋
正式比赛
段位赛
训练棋
AI对局
```

用户可以配置：

> 正式比赛错误优先级更高。

不是因为AI评价不同。

而是因为：

> 这类错误对用户目标更加重要。

---

# 十七、Mastery Modifier

如果同一个问题：

### 复盘时已经自行纠正

优先级降低。

### 一周后仍答错

提高。

### 后续实战再次复发

明显提高。

这样系统真正开始考虑：

> **学会没有。**

---

# 十八、Diversity Modifier

防止 Top5 变成：

```text
83手
85手
87手
89手
91手
```

而这五手实际上属于同一场战斗。

需要新增：

> `problem_cluster_id`

同一局部/同一错误链：

默认最多显示：

> 1～2个代表节点。

---

# 十九、每盘默认只展示5个学习节点

不再显示：

> “本盘有23手值得复盘。”

普通学习模式：

```text
本盘重点学习：5处
```

例如：

```text
47手   方向          4.1目
82手   弱棋          6.3目
103手  攻击/防守     8.9目
151手  先后手        3.7目
201手  官子          5.0目
```

另外提供：

> 查看全部AI问题手

放入：

> 高级研究模式。

---

# 二十、当前最需要重做的功能三：

# 错题不能用“Top3 = 基本正确”判分

现在 `mistake_book.py` 的 `grade_attempt()`：

* 命中首选 → good
* AI排名前3 → hard
* 其他 → again

这是目前非常值得修改的一点。

围棋不存在：

> AI第三选就是正确，第四选突然错误。

例如：

```text
A   -0.0目
B   -0.2
C   -0.3
D   -0.4
E   -0.6
```

第四选完全正确。

反过来：

```text
A    0
B   -4.8
C   -6.2
```

第二选已经很差。

因此：

> **排名不能作为正确性的核心标准。**

---

# 二十一、新增 candidate_assessment.py

所有用户试下的棋都转成：

```text
CandidateAssessment
```

字段：

```text
move
score_loss
winrate_loss
ai_rank
human_prior_current
human_prior_stronger
complexity
assessment
```

assessment：

```text
best
excellent
acceptable
questionable
bad
```

---

# 二十二、动态“合理答案”标准

第一版可以采用：

### AI最佳

```text
≤0.2目
```

### 优秀

```text
≤0.8目
```

### 合理

```text
≤1.5目
```

### 当前水平可接受

根据：

* 用户棋力；
* Human SL；
* 局面复杂度

动态放宽，例如：

```text
≤2.0目
```

### 明显问题

超过动态阈值。

这里的数字是产品初始参数：

> 后续应该通过真实数据和强棋手标注校准。

---

# 二十三、非常重要：

# 用户下的棋不在Top5怎么办

不能直接：

> 错。

正确流程：

用户试下：

```text
P8
```

如果父局面现有候选没有P8：

> 单独向KataGo请求强制分析P8。

KataGo Analysis Engine支持 `allowMoves` / `avoidMoves` 控制根节点搜索。

得到：

```text
P8 -0.7目
```

那么：

> 判定合理。

这会极大降低错题系统的假阴性。

---

# 二十四、重新设计“主动复盘”

当前项目已经存在：

> 阶段训练。

这很好，但未来核心训练应该增加：

> **单关键局面主动复盘。**

流程：

## Step 1

跳到第87手之前。

完全隐藏：

* 推荐点；
  -胜率；
  -目差；
  -问题标签；
  -变化图。

---

## Step 2

只显示：

> 这一步值得重新考虑。

> 如果现在再下一次，你会走哪里？

---

## Step 3

用户实际落子。

例如：

```text
Q11
```

---

## Step 4

分析用户重选。

结果：

```text
实战 R10   -7.3目
重选 Q11   -0.6目
AI   P9     0目
```

---

# 二十五、主动复盘结果分成四类

### Corrected

重新选择已经合理。

说明：

> 用户现在能够独立纠正。

---

### Improved

例如：

```text
实战 -8
重下 -2.5
```

有进步，但尚未完全掌握。

---

### Repeated

再次选择类似错误。

这是：

> 真正知识盲区。

---

### Alternative Correct

没有找到AI第一选，但找到另一个完全合理方案。

判定：

> 正确。

---

# 二十六、用户可增加一个极低摩擦的“当时想法”

这个功能建议做，但默认可跳过。

在揭晓AI之前问：

> 你刚才主要在考虑什么？

按钮：

```text
攻击
防守
做活
围地
脱先
官子
计算
不确定
```

这比语言模型凭空猜：

> “你当时一定是想攻击。”

可靠得多。

以后错误诊断可以结合：

> 用户自述 + KataGo证据 + 语言模型推理。

---

# 二十七、重新设计错误分类体系

当前 `move_quality.py` 只有8种问题标签：

* 布局方向；
* 官子大小；
* 优势保持；
* 劣势胜负手；
* 脱先时机；
* 过分；
* 缓手；
* 死活判断。

这里存在一个结构问题：

有些是：

> 技术领域。

有些是：

> 局势状态。

有些是：

> 行为结果。

最好拆成两个维度。

---

# 二十八、维度A：技术错误

第一版建议：

```text
reading            计算
life_death         死活
shape              棋形
direction          行棋方向
weak_groups        弱棋/轻重
attack_defense     攻击防守
sente_tenuki       先后手/脱先
whole_board        全局判断
endgame            官子
```

第二阶段再加：

```text
joseki
invasion
reduction
ko
```

---

# 二十九、维度B：决策习惯

以后再加入：

```text
automatic_reply
自动跟应

overplay
过度攻击

over_defense
过度防守

local_obsession
过于局部

unwilling_to_sacrifice
不愿弃子

missed_opponent_best
只算自己的棋

advantage_overconservative
优势过度保守

behind_too_passive
落后仍然稳健
```

这才真正接近：

> 个人思维习惯。

---

# 三十、错误分类必须保存证据

不要只保存：

```text
category = attack_defense
```

保存：

```text
category
confidence
evidence[]
source
```

例如：

```text
category:
    weak_groups

confidence:
    medium

evidence:
    ownership shift
    branch comparison
    user intent = attack
    nearby weak own group

source:
    deterministic + coach-model
```

---

# 三十一、现有 evidence_explanation.py 应该保留

这个模块目前做了一件非常正确的事情：

> **证据不足时拒绝编棋理。**

代码明确规定：

只整理本地KataGo数字、棋形意图与双分支结果，没有证据时会明确表示：

> 暂不足以判断具体棋理根因。

这个设计不要删除。

反而应该升级成：

> **整个语言模型解释系统的事实入口。**

---

# 三十二、新增 EvidencePacket

新增：

```text
evidence_packet.py
```

输出：

```text
EvidencePacket
```

包含：

```text
game_id
move_no
board_position
move_history

played_move
best_move

score_before
score_after
score_loss

winrate_before
winrate_after

candidate_moves[]
branch_comparison

ownership_before
ownership_after

human_policy_current
human_policy_stronger

user_intent

deterministic_tags

recurrence_history
```

然后所有语言模型：

> **只能看到这个数据包。**

不让每个模型自己从杂乱内部状态猜数据。

---

# 三十三、正式加入可选语言模型教练

当前 v4.38 明确：

> 不接联网大模型。

这个限制作为现阶段非常合理。

下一阶段建议：

> 增加语言模型，但绝不让它进入客观评价链。

架构：

```text
KataGo
   ↓
EvidencePacket
   ↓
Deterministic explanation
   ↓
LLM coach
   ↓
Validator
   ↓
User
```

---

# 三十四、语言模型绝不能做什么

语言模型禁止：

### 自己判断目损

禁止：

> “大约亏8目。”

除非输入明确提供8目。

### 自己修改胜率

### 发明候选手

### 推荐未经过KataGo校验的手

### 把心理推断写成事实

不能：

> “你下这手是因为害怕攻击。”

只能：

> “一种可能原因是……”

### 覆盖KataGo事实

KataGo是裁判。

模型是教练。

---

# 三十五、模型输出使用强制结构

例如：

```text
CoachExplanation
```

字段：

```text
summary

mistake_category
category_confidence

what_happened

why_problematic

likely_reason

transferable_rule

reasonable_moves[]

short_variation[]

uncertainty

evidence_refs[]
```

要求 JSON Schema 验证。

---

# 三十六、模型回答以后再做程序校验

例如模型说：

> 合理选择Q10、P11、R8。

程序检查：

```text
Q10 是否在CandidateAssessment
P11 是否在CandidateAssessment
R8 是否在CandidateAssessment
```

不存在：

> 删除。

模型引用：

```text
损失5.7目
```

程序检查：

> EvidencePacket有没有5.7。

不一致：

> 拒绝回答并使用 deterministic fallback。

这样可以做到：

> **数字幻觉基本归零。**

---

# 三十七、保留无模型模式

非常重要。

如果：

* 没API；
* API失败；
* 模型输出非法；
* 用户关闭联网功能；

仍然显示：

当前：

```text
evidence_explanation.py
branch_comparison.py
candidate_recommendation.py
```

产生的确定性分析。

也就是说：

> LLM增强产品，但不成为产品运行的必要条件。

---

# 三十八、现有 mistake_book.py 应继续保留

当前错题本已经有：

* 自动同步；
* 到期时间；
* again；
* hard；
* good；
* repetitions；
* lapses；
* mastered；
* 延期；
* 复习间隔。

当前算法大致是：

```text
again → 1天
hard  → 上次间隔×1.7
good  → 上次间隔×2.4
```

这已经足够作为V1。

不要立即引入复杂SM-2/FSRS。

真正应该先改的是：

> **“答对”的定义。**

---

# 三十九、错题记录必须增加完整作答历史

现在主要保存：

```text
lastResult
repetitions
lapses
interval
```

应该增加：

```text
attempts[]
```

每次：

```text
date
played_move
score_loss
assessment
ai_rank
human_prior
hint_used
thinking_time(optional)
```

于是以后可以知道：

> 不是只记得最后一次结果。

而是：

```text
第一次 -6.2目
第二次 -2.3
第三次 -0.8
第四次 -0.5
```

这才是真正的学习曲线。

---

# 四十、掌握程度重新定义

不要只有：

```text
mastered=True/False
```

建议：

```text
new
understanding
retained
transferred
unstable
```

含义：

### new

刚发现。

### understanding

立即复盘能纠正。

### retained

几天/几周后还能正确。

### transferred

真实新棋局里类似问题已经明显减少。

### unstable

复习会，但实战继续犯。

最后一种尤其有价值。

---

# 四十一、“实战迁移”是最终核心指标

如果：

> 同一个旧题连续答对3次，

但接下来十盘真实棋：

> 又犯4次同类错误，

那不叫真正掌握。

系统应该告诉：

> **知识已经记住，但尚未迁移到实战决策。**

这将是本产品和普通错题本最大的区别之一。

---

# 四十二、个人画像需要重构

当前 `player_profile.py` 已经有：

* 平均目损；
* 胜率损失；
* Top1/3/5吻合率；
* 黑白差异；
* 三阶段；
* 趋势；
* 问题标签。

这些可以继续保留。

但个人主页应该重新排序。

---

# 四十三、个人主页最重要的指标

第一位：

## 重复错误率

```text
最近30盘：
41%重大错误属于过去出现过的类型
```

---

第二位：

## 严重错误/盘

例如：

```text
3.8 → 3.2 → 2.6
```

---

第三位：

## 平均目损

分别：

```text
最近10盘
最近30盘
最近100盘
```

---

第四位：

## 错误分类

例如：

```text
弱棋       27%
攻击防守   22%
方向       17%
计算       14%
官子       11%
其他        9%
```

---

第五位：

## 主动纠正率

```text
复盘时自己重新下：
68%明显改善
```

---

第六位：

## 延迟保留率

```text
7日复习    76%
30日复习   63%
```

---

第七位：

## 实战复发率

真正关键：

```text
弱棋错误：
过去30盘 18次
最近30盘  9次
```

---

# 四十四、当前“单局段位估计”建议降级

README目前有：

```text
黑 业余6段表现
白 业余5段表现
```

并说明这是“单局表现估计”，不是正式段位，而且当前交叉校准说明主要来自少量样本。

这个功能可以保留。

但：

> **不要成为个人学习主页核心指标。**

放到：

> 高级统计。

因为：

```text
“今天下出了5段表现”
```

娱乐价值较高。

而：

```text
“过去20盘弱棋错误减少了40%”
```

学习价值更高。

---

# 四十五、当前棋风画像暂时降权

目前已有：

> 8维棋风画像。

其依据主要来自 MoveQuality 的问题标签统计。

但当前错误标签本身还比较粗。

因此建议：

### 暂时保留代码。

### UI降级为实验功能。

等新 taxonomy + Human SL + 多盘证据稳定后再重新启用。

否则容易产生：

> “你是一个偏重中央、进攻积极型棋手。”

这种看起来十分专业，却可能只是三盘棋中几次启发式标签的文学加工。

---

# 四十六、成长路线同样暂时降权

`growth_path.py` 当前可以输出：

* 一个主线目标；
* 保留风格；
* 修正习惯；
* 观察项。

逻辑框架是好的。

但是数据源需要升级。

以后它应该基于：

```text
LearningPriority
+
Recurrence
+
Mastery
+
Transfer
```

而不是主要依靠旧棋风标签。

---

# 四十七、阶段训练保留，但不再作为第一训练入口

现有阶段训练非常完整：

* 找最差约36手阶段；
* 用户重下；
* AI应手；
* 隐藏候选；
* 最多训练约40手；
* 最后比较平均目损；
* 给A/B/C/D评分。

这个功能保留。

但产品顺序应该改成：

### 第一层

> 单关键局面主动复盘。

### 第二层

> 错题复习。

### 第三层

> 阶段训练。

因为连续重下30～40手认知负担比较高。

适合专项训练。

不适合每盘棋默认流程。

---

# 四十八、增加“错误链”

第二阶段新增：

```text
error_chain.py
```

目标：

区分：

> 最大爆炸点

和：

> 根源错误。

例如：

```text
63手 留下弱棋
 ↓
84手 被迫补棋
 ↓
107手 丢掉大场
 ↓
129手 落后
 ↓
143手 强行战斗
 ↓
151手 崩盘
```

AI最大的目损：

> 第151手。

真正值得学习：

> 第63手。

---

# 四十九、第一版错误链不用上机器学习

先使用：

### 局部连续性

### ownership持续恶化区域

### 同一棋块相关性

### 问题标签连续性

### 分支结果

### 时间距离

生成：

```text
ProblemCluster
```

然后每个cluster选：

```text
root_problem
largest_loss
representative_position
```

---

# 五十、存储架构不要马上换SQLite

目前项目采用：

* `.kga.json`
* `index.json`
* `mistake_book.json`
* training cache

等本地文件体系，并已有原子写、缓存和内容去重。

对于个人工具：

> 暂时够用。

现在直接迁SQLite：

风险 > 收益。

---

# 五十一、但现在就应该加入 Repository 层

例如：

```text
LearningStore
```

接口：

```text
save_event()
get_events()
get_events_by_game()
get_events_by_category()
save_attempt()
get_due_reviews()
```

第一版：

> JSON实现。

未来：

> SQLite实现。

这样后面迁移不会改整个程序。

---

# 五十二、新核心数据对象：LearningEvent

建议这是整个新版系统最重要的数据结构。

例如：

```text
LearningEvent

id
game_id
move_no
position_key

player_color
played_move

score_loss
winrate_loss

best_move
candidate_assessments[]

human_profile
human_prior_current
human_prior_stronger

complexity

primary_category
secondary_categories
category_confidence
category_evidence

learning_priority
priority_components
priority_version

user_retry_move
retry_score_loss
retry_status

recurrence_cluster

mastery_state

created_at
updated_at
```

以后：

> 所有个人画像都从 LearningEvent 聚合。

---

# 五十三、客观数据和解释数据必须分开

例如：

```text
objective_analysis
```

来自KataGo。

```text
interpretation
```

来自：

* 规则；
* 用户；
* LLM。

绝不能混在一个字段里。

例如：

```text
scoreLoss = 6.3
```

永远不能被语言模型改写。

---

# 五十四、保存版本信息

每个 LearningEvent 增加：

```text
katago_version
model_hash
visits
analysis_config_hash

priority_algorithm_version
taxonomy_version

coach_model
coach_prompt_version
```

以后模型或算法变化以后：

> 可以重新分析。

否则半年以后看历史数据，不知道为什么同一个局面以前标“方向”、现在标“弱棋”。

---

# 五十五、主界面重新定义

当前界面仍然带有较强“研究工作台”气质。README描述右侧主要是：

* 研究；
* 复盘；
* 棋谱；
* 导航。

建议增加：

> **学习模式 / 研究模式**

默认：

> 学习模式。

---

# 五十六、学习模式只保留四个核心入口

## 我的棋谱

显示：

```text
最近棋局
分析状态
重点问题数
是否复盘
```

---

## 本盘复盘

显示：

```text
本局5个学习点
```

---

## 今日复习

显示：

```text
今日8题
```

---

## 我的学习

显示：

```text
最近问题
趋势
正在训练主题
```

---

# 五十七、研究模式保留现有功能

把：

* Ownership；
* Policy；
* Visits；
* 完整候选；
* 长PV；
* 热力图；
* 棋树；
* 高visits深算；
* 分支研究；

全部留在：

> 高级研究模式。

这样无需删除成熟代码。

只是：

> **不让普通学习流程被它们淹没。**

---

# 五十八、单盘复盘界面

顶部：

```text
本局建议学习 5 个局面
```

列表：

```text
① 47手   方向       4.1目
② 82手   弱棋       6.3目
③ 103手  攻防       8.9目
④ 151手  先后手     3.7目
⑤ 204手  官子       5.1目
```

---

# 五十九、进入问题以后

第一屏只显示：

```text
第82手

如果重新下一次，
你现在会怎么下？
```

棋盘可落子。

不要显示AI。

---

# 六十、用户作答以后

显示：

```text
你的实战       R10      -6.3目

你的重选       Q11      -0.7目
               ✓ 合理

AI首选         P9        0.0目
```

然后：

```text
你已经自行修正这个问题。
```

---

# 六十一、下面才显示解释

例如：

## 问题

自身弱棋未安定时发动攻击。

## 为什么

右侧黑棋自身存在薄味，R10让双方形成接触以后，黑棋先需要处理自己的棋，因此攻击无法持续。

## 你重新选择为什么好

Q11先改善自身棋形，因此即使不是AI第一选，依然是很好的实战方案。

## 记忆原则

> **攻击之前先比较双方谁更弱。**

---

# 六十二、增加“我的一句话”

用户可以保存：

```text
“自己有弱棋的时候不要着急攻击。”
```

这个文本进入错题。

自己的语言：

> 往往比模型写的五段解释更容易记住。

---

# 六十三、错题界面

不再只是：

```text
到期8题
```

而显示：

```text
今日复习 8

弱棋       3
先后手     2
方向       2
官子       1
```

---

# 六十四、个人主页

建议最终只突出：

```text
最近30盘

严重错误/盘      3.4 → 2.8
平均目损         0.71 → 0.59
重复错误率       44% → 31%
主动纠正率       67%
7日保留率        73%
```

然后：

```text
当前第一训练主题：
弱棋管理
```

---

# 六十五、训练主题一次只给一个

不要：

> “你的六大问题。”

growth path目前已有“一个主线目标”的思想，这一点应该继续坚持。

例如：

> **未来一阶段只关注：攻击前检查自身弱棋。**

配：

```text
最近出现 11次
平均损失 5.2目
复发率   48%
```

这才像私人教练。

---

# 六十六、多模型测试正式成为开发基础设施

你想测试不同模型，这是这个项目非常适合做的事情。

不要只手工复制粘贴几个局面。

新增：

```text
benchmark/
```

结构：

```text
benchmark/
    positions/
    evidence_packets/
    reference_labels/
    outputs/
    ratings/

    run_benchmark.py
    validate_output.py
    score_models.py
```

---

# 六十七、冻结测试输入

例如选：

> 50个真实个人问题局面。

每个生成固定：

```text
EvidencePacket.json
```

所有模型收到：

> 完全相同的数据。

否则无法公平比较。

---

# 六十八、模型统一任务

所有模型必须返回同一个：

```text
CoachExplanation
```

测试：

### 错误分类

### 棋理解释

### 可迁移原则

### 合理候选解释

### 不确定性

---

# 六十九、模型100分评分体系

## 围棋事实准确：30

不能与KataGo矛盾。

## 教学解释：25

是否真正讲清楚为什么。

## 个性化：15

是否结合：

* 棋力；
* 历史问题；
* 重复次数。

## 可迁移性：15

是否产生下次可以使用的判断原则。

## 不确定性控制：10

证据不足是否承认。

## 简洁度：5

---

# 七十、三个硬性幻觉指标

额外单独统计：

## Numeric Hallucination

是否编：

```text
目数
胜率
概率
```

目标：

> 0。

---

## Move Hallucination

是否推荐：

> 未被KataGo验证的明显坏棋。

---

## Evidence Contradiction

是否：

> 解释与Ownership/PV/分支结果冲突。

---

# 七十一、建立人工基准答案

建议挑：

> 20～50个典型局面。

由：

> 比自己明显更强的棋手或教练

人工标注：

```text
核心问题
合理候选
学习原则
```

然后模型比较。

没有人工标注：

> 很容易出现几个语言模型互相评判谁更像围棋老师。

这多少有点让几只鹦鹉互相检查语法。

---

# 七十二、Milestone 0：先建立安全基线

在动功能之前完成：

### 冻结当前v4.38

创建明确版本标签。

### 跑全部现有测试

仓库文档已经包含大量纯逻辑和UI测试，因此应把它们作为改造回归基线。

### 保存测试SGF

固定：

```text
10～30盘
```

作为回归集。

### 保存KataGo输出

以后：

> UI、学习算法改变不能偷偷改变基础目损计算。

---

# 七十三、Milestone 1：架构稳定

只做工程基础。

完成：

### 新增 `LearningEvent`

### 新增 `LearningStore`

### 新增版本字段

### 新增 EvidencePacket

### 新代码不再进入app.py

暂时：

> 用户体验基本不变。

验收：

> 原有功能全部通过。

---

# 七十四、Milestone 2：重做问题手排序

新增：

```text
learning_priority.py
```

替代：

> 单纯score loss Top5。

此时先不用Human SL。

使用：

```text
severity
recurrence
learnability
diversity
```

验收：

人工标注30盘。

目标：

> Top5覆盖大多数人工认为真正值得复盘的问题。

第一版内部目标可以：

> ≥80%。

---

# 七十五、Milestone 3：单关键局面主动复盘

新增：

> 隐藏答案 → 用户重新落子 → KataGo评价。

替代：

> 一上来显示AI推荐。

验收：

所有关键节点都能：

```text
进入
重下
分析
比较
保存
```

---

# 七十六、Milestone 4：重做答题判定

废除：

> Top3 = 正确。

改用：

> 实际目损。

如果用户试下不在现有候选：

> 自动强制分析该手。

验收：

准备：

```text
AI第1
AI第2
AI第4但0.4目
AI第2但5目
榜外但合理
```

等边界测试。

---

# 七十七、Milestone 5：新版错误分类

先完成：

> 9个技术类别。

让：

```text
MoveQuality
```

只负责：

> 客观质量。

让：

```text
taxonomy.py
```

负责：

> 错误解释分类。

不要继续把：

> 数值质量

和：

> 棋理原因

绑死在同一个模块。

---

# 七十八、Milestone 6：重新设计错题本

增加：

```text
attempt history
mastery state
transfer state
```

保留当前间隔算法。

不急着换FSRS。

验收：

能回答：

> “这个问题以前做过几次，每次下了什么，现在属于记住还是已经实战迁移？”

---

# 七十九、Milestone 7：接 Human SL

修改：

```text
katago_client.py
config_manager.py
analysis request builder
```

新增：

```text
human_sl.py
```

获取：

```text
rank_1d humanPolicy
rank_3d humanPolicy
```

等等。KataGo官方支持双模型方式，并允许仅用Human模型提供Policy、主模型继续负责正常强棋分析。

这是推荐架构：

> **主模型负责棋力事实。**

> **Human SL只负责人类选点模型。**

不要拿 Human SL 模型的胜率/目差替代主KataGo模型，因为官方也提醒人类模型的分数和胜率可能存在额外偏差。

---

# 八十、Milestone 8：语言模型教练

先不要马上联网。

先建立：

```text
CoachProvider
```

接口：

```text
explain(EvidencePacket)
```

实现：

```text
DeterministicCoach
```

然后再：

```text
OpenAIProvider
ClaudeProvider
GeminiProvider
LocalModelProvider
...
```

系统核心不关心是哪一家模型。

---

# 八十一、Milestone 9：模型Benchmark

所有Provider运行：

> 相同50局面。

自动检查：

```text
schema
numbers
moves
evidence
```

再进行人工评分。

最终得到：

```text
Model A
事实      98
解释      86
可迁移    91
成本      ...

Model B
事实      99
解释      92
可迁移    84
...
```

这样才真正能够测试模型性能。

---

# 八十二、Milestone 10：长期学习画像

重构：

```text
player_profile.py
style_profile.py
growth_path.py
```

使它们消费：

> LearningEvent

而不是只消费：

> MoveQualityResult。

核心增加：

```text
recurrence
mastery
transfer
```

---

# 八十三、Milestone 11：错误链

新增：

```text
problem_cluster.py
error_chain.py
```

把：

> 一连串后果

归并成：

> 一个根源错误。

---

# 八十四、Milestone 12：UI学习模式

最后再大改UI。

不是先改。

新的默认导航：

```text
棋谱
复盘
复习
我的学习
```

原有复杂分析：

> 收到“研究模式”。

---

# 八十五、明确现在不做什么

## 不改KataGo核心

没有意义。

## 不开发围棋服务器

完全偏题。

## 不做实时对局分析

避免形成作弊工具。

## 不做职业赛事直播

偏题。

## 不做完整SGF编辑器升级

当前已经够用。

## 不做多围棋AI引擎

KataGo够用。

## 不立即Web化

先证明学习闭环。

## 不立即云同步

先把个人本地版本做正确。

## 不立即SQLite化

先增加Storage abstraction。

## 不立即做精确段位估计

数据依据不足。

## 不立即强化“棋风人格”

先把基础错误分类做可靠。

## 不立即增加十种AI陪练人格

阶段训练已经足够。

---

# 八十六、现有模块最终处理方式

| 当前模块                          | 处理    | 原因             |
| ----------------------------- | ----- | -------------- |
| `board.py`                    | 保留    | 基础棋盘           |
| `movetree.py`                 | 保留    | 状态树            |
| `katago_client.py`            | 小改    | 增Human SL      |
| `analysis_guard.py`           | 保留    | 异步安全           |
| `analysis_queue.py`           | 保留    | 批量历史分析核心       |
| `review.py`                   | 保留/收敛 | 客观分析           |
| `move_quality.py`             | 重构职责  | 只做质量，不负责完整棋理   |
| `candidate_recommendation.py` | 重点重构  | 从启发式变真正棋力适配    |
| `problem_drill.py`            | 重点重构  | 从目损TopN变学习TopN |
| `mistake_book.py`             | 重点升级  | 增作答历史/掌握/迁移    |
| `training.py`                 | 保留    | 专项阶段训练         |
| `training_analysis.py`        | 扩展    | 接Mastery       |
| `player_profile.py`           | 二阶段重构 | 接LearningEvent |
| `style_profile.py`            | 暂时降权  | 当前标签证据不足       |
| `growth_path.py`              | 暂时降权  | 等新画像           |
| `branch_comparison.py`        | 强保留   | 高价值证据          |
| `evidence_explanation.py`     | 强保留   | LLM事实防火墙       |
| `deep_verification.py`        | 强保留   | 重要结论复核         |
| `game_library.py`             | 保留    | 个人历史核心         |
| `project_store.py`            | 保留    | 缓存价值很高         |
| `app.py`                      | 渐进拆分  | 已过于庞大          |

---

# 八十七、最优先应该动的文件

如果真正开始修改代码，我建议顺序严格是：

```text
1. learning_event.py            新建

2. learning_store.py            新建

3. learning_priority.py         新建

4. candidate_assessment.py      新建

5. problem_drill.py             改

6. mistake_book.py              改

7. evidence_packet.py           新建

8. move_quality.py              收敛职责

9. player_profile.py            改

10. katago_client.py            加Human SL

11. human_sl.py                 新建

12. coach_provider.py           新建

13. coach_schema.py             新建

14. app.py                      只负责接UI
```

不要第一步就：

> 重写app.py。

---

# 八十八、测试体系增加四层

## 第一层：基础引擎回归

现有测试全部保留。

---

## 第二层：学习算法单元测试

新增：

```text
test_learning_priority.py
test_candidate_assessment.py
test_learning_event.py
test_mastery.py
test_human_sl.py
```

---

## 第三层：Golden Position

例如：

```text
tests/fixtures/learning_positions/
```

固定30～50个真实局面。

确保：

> 算法升级以后Top问题不会无缘无故完全变化。

---

## 第四层：模型Benchmark

独立：

```text
benchmark/
```

不要和产品测试混在一起。

---

# 八十九、必须建立“不变量”

任何以后模型都不能破坏：

### Point Loss

必须来自KataGo。

### 用户走子

必须合法。

### 推荐手

如果宣称合理：

> 必须经过KataGo分析。

### 目数

不能由LLM生成。

### 胜率

不能由LLM生成。

### Human Policy

必须来自Human SL。

### 错误分类

允许不确定。

---

# 九十、系统最终的三个可信度级别

每个结论展示：

## 事实

例如：

> 实战比AI首选损失6.3目。

来源：

> KataGo。

---

## 强推断

例如：

> 主要问题可能是自身弱棋未处理。

来源：

> Ownership + 分支 + 标签 + 模型。

---

## 学习建议

例如：

> 下次攻击前先检查双方弱棋。

来源：

> 教练层。

用户应该能够知道：

> 什么是事实，什么是解释。

---

# 九十一、个人隐私原则

既然目标是：

> 自己的历史实战，

本地优先是当前项目非常有价值的特性。

建议默认：

```text
SGF
KataGo分析
画像
错题
```

全部保留本地。

如果调用语言模型：

只上传：

> 当前需要解释的EvidencePacket。

而不是：

> 整个个人棋谱数据库。

---

# 九十二、最终V1.0改造完成标准

我认为真正的新版本完成，不以：

> “新模块都写出来”

判断。

而应该可以完成下面这一次体验：

### 用户导入30盘自己的棋。

系统自动分析。

### 打开其中一盘。

系统说：

> 本局最值得学习5处。

### 进入第一处。

完全隐藏答案。

### 用户重新下一手。

系统判断：

> 合理 / 改善 / 重复错误。

### 如果不在原AI候选。

自动强制分析。

### AI解释：

> 问题是什么。

### 保存：

> 错误类别。

### 系统发现：

> 过去15盘已有4次类似错误。

### 错题进入复习。

### 一周以后再次出现。

### 用户答对。

### 两个月之后真实棋局没有再犯。

系统最终：

> 把这个问题从“反复问题”降为“已明显改善”。

如果这个完整闭环成立：

> **产品第一阶段真正完成。**

---

# 九十三、最终产品定位

改造完成以后，项目的README第一句话最好不再是：

> KataGo个人围棋分析器。

而变成：

> **基于个人历史实战的围棋学习系统。**

下面一句：

> KataGo负责客观判断，个人历史决定学习重点，AI教练负责解释，而系统持续追踪你是否真的改掉了过去的错误。

这句话应该成为所有新功能是否应该加入项目的判断标准。

如果一个功能不能帮助：

```text
发现问题
理解问题
训练问题
减少复发
```

四件事中的至少一件：

> 默认不做。

这样这个项目才不会重新长回一个拥有72个按钮的KataGo驾驶舱。
