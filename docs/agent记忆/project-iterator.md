# agent 记忆 —— project-iterator（迭代规划调度）

> 动态事实区：由 project-iterator 每轮结束追加。定义文件（.zcode/agents/project-iterator.md）永不写死数字，一切基准以本文件最新记录为准。

## 2026-08-21（初始化）
- 单一职能制确立：本 agent 只做规划/调度/跟单，专门级审查路由给对应 agent。
- 基准快照：app.py 万行级 · ui/ 六模块 + 四页面 · 7 个单一职能 agent 就位。
- 待办路由登记：（空——各 agent 记忆文件的"建议调度"项在此跟单）
