# _接力板 —— 跨 agent 转记事项

> 越界发现统一登记处（发现者 / 证据 / 建议接手者 / 状态）。每波开始先读板领任务，完成即销。
> 状态：待领 · 进行中（标注接手者）· 已销（保留一行存档）

| # | 事项 | 证据 | 建议接手者 | 状态 |
|---|---|---|---|---|
| 1 | ~~test_ui_design 收口~~ | — | continuous-optimizer | 已销（并行会话收口，优化轮4确认通过） |
| 2 | ~~GAP-3 官子专项训练：本地棋谱终局段自动生成收束题~~ | docs/产品对标与整改清单.md v2 | code-auditor（生成逻辑）+ ui-modernizer（practice 入口） | 已销（2026-08-21：生成逻辑 endgame_drill.py + UI 全链接线完成——practice 页入口卡片 + 复盘页「官子训练」按钮 + 训练窗口/判分/守卫（独立 endgame 独占模式），test_ui_smoke 新增 test_endgame_drill_ui 全链场景，全量回归 56/56 绿，详见 ui-modernizer.md） |
| 3 | katago_client.stop() 不复位 started/proc | app.py:1886/1922 每次新建实例，非活 bug | continuous-optimizer | 备忘（P2，加"重启引擎"功能时必做） |
| 4 | ~~app.py 两处静默吞错~~ | app.py:6713/7143（动态定位） | continuous-optimizer | 已销（优化轮4：改 stderr 留痕） |
| 5 | dialogs.py 全 tk 控件 CTk 化 | ui-modernizer 盘点 | ui-modernizer（串行写波） | 已销（2026-08-21：约 60 控件 CTk 化，全部回归通过，详见 ui-modernizer.md） |
| 6 | ~~B 区 app.py tk→CTk 迁移（基准 tk.Label 103 → 波3扫描 82 → 波4第一批后 67 宽口径/53 构造位 → 波7第二批后 34 宽口径/20 构造位 → **波12第三批后 17 宽口径/2 构造位，任务销单**：第三批消化 18 处＝个人画像窗 9（前置探针实证 test_ui_smoke 的 isinstance(tk.Label) 收集器命中 CTkLabel 内部 tkinter.Label，直接迁移）+ 候选行 4（_keep_custom_bg 包裹保留——降级路径仍依赖防刷白底，CTk 路径 _polish_card 跳过 CTkLabel 故冗余无害）+ 5 处更新点同步转 .configure/_label_set + brand/workbench_title 2 + 棋局编辑卡 2 + 粘贴SGF 1；**tooltip 1 处刻意保留 tk.Label**（relief=solid 反色浮窗，CTkLabel 无 relief 语义且瞬态浮窗 ROI 低风险高）；剩余 2 构造位＝工厂降级位（_make_label 无 CTk 真实降级路径，保留即设计）+ tooltip（如上）。降级探针 12/12 OK，全量 57/57 绿，详见 ui-modernizer.md）~~ | ui-modernizer 盘点 | ui-modernizer（串行写波） | 已销（波12第三批 2026-08-22 交付：B 区 tk.Label→CTkLabel 三批累计 103→2 实构造位，新代码一律走 `_make_label`/`_label_set` 工厂的规矩维持） |
| 7 | ~~【用户直提·最高优先】主窗口布局重构~~ | 用户 2026-08-21 原话：'棋盘布局不对，棋盘可以增大，其他功能可以做成边栏然后适当密集缩小' | ui-modernizer（串行写波，独占 app.py） | 已销（2026-08-21：边栏 480→396、appbar 64→42、BOARD_SCALE 0.84→0.95，同窗棋盘 1600×900 下 606→704(+16.2%)、1200×800 下 488→606(+24.2%)，55/55 回归全绿，详见 ui-modernizer.md） |
| 8 | ~~store 家族默认路径导入期固化（def f(path=DEFAULT_PATH)）~~ | profile_store.py:179/222/276/293/471/517 等 7 处 + style_store/learning_store/mistake_book/deep_verification 全体同款；取证实验证明 set_path 式重定向被忽略、真实写穿生产 profile_cache.json（已即时恢复） | code-auditor | 已销（2026-08-21 波5：5 个 store 全部改为调用时解析——默认参数 None + _resolve_path；统一 usage_log 同款 set_path/get_path/default_path 约定；默认路径派生自 game_library 当前属性（gl 重定向自动生效，W29 隔离修复）；无重定向时生产路径零变化（已断言）。回归：5 个 test_*.py 各加重定向用例（style_store 首建测试）+ test_adversarial 225 序列 + self_review 21OK/0P0/0P1，详见 code-auditor.md 第五波） |
| 9 | ~~app.py open_player_profile 缺 profile None 守卫（降级不完整 P1）~~ | app.py ~9688（波5 工作副本行号，HEAD:9005 同款）：`get_or_rebuild()` 空库返回 None 后直接 `profile.problem_tag_distribution.keys()` → AttributeError。真实用户面 bug：全新安装/0 盘有画像摘要时点「个人画像」即崩。此前被 #8 固化 bug 掩盖——W29 测试靠生产 index.json 泄入才不崩；#8 修复后 W29 在空 tmp 库上必崩（test_workflow_sim 当前唯一红） | code-auditor（下一轮 app.py 无并行冲突时）或当前 app.py 写波 agent | 已销（2026-08-22 波6：profile None 时开空态说明窗（`_empty_card` 范式「画像尚未生成」+引导分析一盘棋）并 return，窗口仍登记 _profile_win 保关窗/重开链；标题走 _make_label 工厂。回归锚：W29 补 2 断言（空态窗文本遍历+引用链），W29 转绿，全量 57/57 绿，详见 优化轮报 第16轮） |
| 10 | ~~官子/drill 揭示锁盘态 hover 幽灵子仍显示（低危·反馈不一致）+ `_endgame_reveal(answer_coord=...)` 死参数（低危·代码卫生）~~ | app.py `_on_board_motion` 的 can_preview 不查 drill/endgame 揭示态——已揭示棋盘锁定（点击只有提示），鼠标划过仍画当前行棋方幽灵子，暗示可落子（drill/endgame 同款）；`_endgame_reveal` 的 answer_coord 参数从未被函数体使用 | trigger-flow-auditor（hover，下轮顺手）+ code-auditor（死参数） | 已销（2026-08-22 波6：can_preview 补与 _on_board_click 同源两判（endgame/drill 揭示锁定态不画幽灵子）；死参数删除（按钮直接绑方法+尾调用去参）。回归锚：W35 hover 三态断言（普通有/drill揭示无/官子揭示无），全量 57/57 绿） |
| 11 | ~~drill/官子两张候选表的同值三元 + 0.0 显示格式不一致（低危·代码卫生/文案）+ grade_choice 与 grade_quiz 的"选了实战着法"键名分叉 isPlayed/isActual（低危·命名）~~ | app.py:7467/7472（_drill 表）与 7952/7955（_endgame 表，复制时连带继承）：`("%d" % c.visits) if c.visits else "0"` 两分支渲染相同；`("%.1f" % c.score_loss) if c.score_loss else "0"` 把 0.0 显示成 "0" 而非 "0.0"；isPlayed 改名会动 app.py 消费契约，code-auditor 本轮未做 | continuous-optimizer | 已销（2026-08-22 波8 第17轮：核实非"漏写分支"而是 falsy 判空写法错——候选字段恒为数值（builder `_num(…,0)` 兜底），visits 三元两分支同输出（else 死代码）、loss 三元把 0.0 显示成 "0"；5 处（drill 3 + endgame 2，board 口径 4 实漏记 drill 表 wrloss）全部去三元直接格式化；键名统一 endgame 侧 isPlayed→isActual 3 处（endgame_drill.py grade_choice + app.py 两消费点），grade 字典纯内存无兼容面。回归锚：test_problem_drill_ui/test_ui_smoke 0.0 渲染断言 + test_endgame_drill 键名断言 + W36 场景，全量 57/57 绿，详见 优化轮报 第17轮） |

| 12 | ~~官子训练作答无 LearningEvent 回写（不进学习闭环，画像/错题本看不到官子表现）~~ | 波9 规划（用户批准自动执行） | code-auditor（事件模型+store）→ app.py 接线串行 | 已销（2026-08-22 波10：事件模型 kind=endgame_drill 独立类别落地——learning_event.py 新增 kind/drill_kind/drill_value 字段 + endgame_event_id 命名空间隔离（game_id 原值，remove_game 联动仍生效）+ from_endgame_drill 构造器（primary_category 刻意留空，不进 taxonomy/复发统计；mastery 恒 new、无 review_due，不进 SRS——D5 同源边界）；learning_store 新增 record_endgame_drill_attempt（get-or-create + 追加 attempts）+ get_events/get_events_by_game kind 过滤 + store_stats 官子单列；app.py _endgame_free_answer 判分后落库（_endgame_persist_answer，失败 stderr 留痕）+ 时间轴/学习优先级上下文两处消费点 kind=problem 防污染；画像呈现走 learning_profile.summarize_endgame_drills 子汇总 + format_learning_summary 一行「官子训练：累计作答 N 题，答对率 X%，平均选点目损 Y」（个人画像窗 learn_card 与学习中心同源消费）+ learn_card 门控放宽；错题本刻意不进（官子≠错题，进本污染 SRS 语义）。回归锚：test_learning_event 官子构造 6 断言 + test_learning_store run_endgame_drill_events 15 断言（id 隔离/重复作答只追加/kind 过滤/不进到期队列/复发索引无污染/画像一行/删除联动）+ test_ui_smoke 官子场景作答落库 2 断言；全量 57/57 绿 + self_review 21OK/0P0/0P1 + doc_sync 一致。遗留子项见 #14） |
| 13 | ~~备份有产出无恢复 UI（W33 实证产物可加载，用户无入口——数据安全闭环缺一半）~~ | W33 场景证据 | code-auditor 或 ui-modernizer（串行 app.py/dialogs） | 已销（2026-08-22 波11 ui-modernizer：backup.py 新增 RestoreError/list_backups/restore_backup——全量校验（可开/CRC/zip-slip 路径逃逸/有库文件/index 可解析）→ 磁盘余量预检 → 同级暂存目录解包 → backups/ 搬进暂存（历史备份不因恢复丢）→ 现库+设置转存 pre_restore 副本（同秒不撞名、滚动保留 2 份）→ 原子改名顶上（失败回滚）→ 设置 .tmp+os.replace 写回；拒绝路径一律不动原库。UI：ui/dialogs.py 备份与恢复窗口（列表=日期/大小/局数/完好性，ttk.Treeview 保留 B 区妥协方案）+ 系统设置页新增「数据安全」区块入口（690→800 高度），app.py 仅 3 属性+4 薄委托；二次确认 askyesno 明确警告不可撤销，成功提示重启刷新内存态。回归锚：test_backup 新增 3 用例（正常往返/损坏与非 zip 与 zip-slip 与无库文件拒绝后现场原样/缺失报错+pre_restore 滚动清理）+ test_workflow_sim 新增 W38 备份恢复UI全链（未选中提示→确认取消现场不动→确认恢复库+设置回去+pre_restore 存破坏现场+损坏备份标坏被拒+关窗清引用）；全量 57/57 绿 + self_review 20OK/0P0/0P1 + doc_sync 一致，详见 ui-modernizer.md） |
| 14 | 官子画像呈现深化（波10 #12 遗留）：① 画像一行已落 learning_profile（format_learning_summary），player_profile.py（棋力画像，数据源 MoveQuality）未纳入官子分量——若要在"优先训练/详细分析"区做官子专项卡片需跨数据源接线（棋谱库终局段表现 × 学习事件作答），改动面大按 #12 任务书"先做事件入库+画像一行"原则砍掉；② 学习中心状态条（自建 parts，不走 format_learning_summary）尚未展示官子摘要；③ 官子训练总结窗可加"本次作答已记入学习画像"提示（用户感知闭环） | 波10 #12 收尾记录 | ui-modernizer（②③ UI 面）或 product-analyst（① 取舍） | 待领 |
## 已销存档

- 2026-08-21 W16 测试注入失效（dialogs.py 外迁破坏）→ user-sim-adversary 已修（双点位注入）
- 2026-08-21 接力板 #8 原行（profile_store 默认路径 def 期固化，备忘级 P3）→ 波 5 升级为实修，见下表 #8 已销记录。

