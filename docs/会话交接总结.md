# 会话交接总结（2026-08-20 ~ 08-21）

> 新对话开场可直接让 AI 读本文件。项目：D:\katago，主应用 analyzer/app.py
> （Python/Tkinter + KataGo 引擎，单机复盘教练定位）。
> 本会话（承接 08-19/20 上半场）= continuous-optimizer 连续十一轮 R4-R14。

## 一、九个可复用 Agent（~/.zcode/agents/）

| agent | 职责 | 触发话术 |
|---|---|---|
| code-auditor | 代码层审查（互斥/单位/魔数/死代码），默认直接修 | "审查逻辑不顺" |
| product-analyst | 产品对标（涨棋网/星阵/KaTrain/LizzieYzy） | "产品对标分析" |
| trigger-flow-auditor | 触发流程审查（进入/退出/恢复闭环、状态残留） | "审查触发流程" |
| red-team-fuzzer | 对抗不变式盲点（一次性种子） | "找对抗盲点" |
| ui-rewrite-designer | 高标准 UI 重写（三工作包） | "UI 重写" |
| ui-modernizer | 持续 UI 现代化（五方向 ROI 清单） | "继续 UI 现代化" |
| project-iterator | 规划式迭代（扫描→清单→批准→执行） | "继续迭代" |
| user-sim-adversary | 用户仿真对抗（真实工作流场景） | "继续仿真" |
| **continuous-optimizer** | **总引擎：仿真轮↔增强轮自驱动循环**（S2.5 四维寻优：鲁棒性/解耦/功能/UI） | **"继续优化"** |

⚠️ 本机环境注意：子 Agent 启动可能报 `modelCode 不存在`（环境侧模型配置问题，
重试仍失败即按其完整流程在主会话直接执行——R4-R13 即如此执行）。

## 二、测试与仿真基础设施（analyzer/）

- **adversarial_harness.py**：`make_headless_app`（单例 root + 弹窗 mock）/
  `clean`（段间重置，**已含 9 本 rid 挂账字典清空 + guard.clear**）/
  `seed_fixture(blunder|analyzed|simple)` / `pump_after_callbacks`
  / **`create_tk_root`（root 创建健壮工厂，闪烁根治核心）**
- **invariants.py**：I1-I14+ 不变式（互斥/换谱清场/图层/导航拦截/窗口生命周期/nid 缓存）
- **test_adversarial.py**：12 棋盘动作 pairwise 144 序列（独立脚本，非 pytest）
- **test_workflow_sim.py**：**W1-W20 用户仿真 20 场景**（独立脚本 `python analyzer/test_workflow_sim.py`）：
  W1 复盘闭环 / W2 窗口轰炸 / W3 连打边界 / W4 队列让路 / W5 回流+切谱 /
  W6 桩引擎队列并发 / W7 陈旧结果防护 / W8 键盘×模式矩阵（含 F1 泄题强断言）/
  W9 真实 SGF 全链 / W10 训练预取竞态 / W11 引擎生命周期销账 /
  W12 棋谱库×换谱谱系 / W13 drill×队列交错 / W14 坏输入异常路径 /
  W15 完整训练闭环 / W16 错题复习闭环 / W17 队列跨实例持久化 /
  W18 前台整盘×曲线实时 / W19 引擎死亡×队列续跑 / W20 曲线×导航双向联动
- **桩引擎谱系**（都在 test_workflow_sim.py 内）：
  FakeClient（即时回流）/ ManualClient（受控回流）/ SgfScanClient
  （黑每手-2.5目、制造问题手）/ **TrainingClient（候选必须是空点**——按查询
  moves 推导占用集，否则 AI 应手选到占点训练卡死）/ DyingManualClient
  （受控回流+可控死亡）
- **关键仿真经验**：① poll 定时器重排后需真实时间——用 `advance()` 不用
  pump×3；② 换 app 实例的场景（W17）后 run() 循环自动从 `_app_instance` 重取；
  ③ mistake_book 路径是导入时绑定默认参数——测试用 `app.list_mistake_items`/
  `record_graded_attempt_mb` 模块别名注入隔离
- 底层单测：test_board / test_movetree 等；pytest 全量 200 收集（不含两个脚本）

## 三、本会话完成的主要工作（R4-R14 十一轮）

### R14（08-21）性能维度深挖 + W20
- **批量回流 O(n²) 修复**：实测 300 手整盘分析累计 4.8s UI 阻塞 → 0.9s（5.3×）
  - review.py 新增 `CachedReviewReport` 快照子类（实例缓存
    mainline/候选排序/_eval）；**基类保持活读契约**（#24-1 回归测试
    拦截过缓存放基类的错误方向——契约测试价值实证）
  - app.py `_update_review_state(light=True)` 批量轻量档：逐手只刷
    曲线/失误榜/当前 loss，评分/画像/解说完成时补全量；顺带消除
    每回流一次的 `_load_index()` 读盘
  - 性能基准资产：`analyzer/bench_review.py`（合成树 A/B 实测可复跑）
- W20 曲线×导航双向联动场景（仿真 19→20）
- 静态收口：`ReviewReport._move_infos` 静态调用两处 → `_sorted_move_infos`

### 修复的 9 个真 bug（R4-R13，全带 file:line 证据，详见 docs/优化轮报.md 对应轮）
1. 渲染复盘表格就**静默拉起 KataGo**（_render_review 程序化恢复选中→auto_start）
2. **快速预扫 TypeError 全链损坏**（TRAINING_SPEED_MODES 元组当 visits 传，HEAD 即坏）
3. **盲测 drill 按 F1 直接泄答案**（show_hint 无 drill 守卫，W8 弱断言没拦住）
4. 换谱后质量环泄漏（I5c：_current_quality_result 与 loss 门控不对称）
5. **垃圾 SGF 静默清盘**（import_sgf 空 raw_nodes 不报错→当前棋局被换空盘）
6. **训练报告窗口 tagconfigure 崩断**（ttk.Treeview 无此方法，上线即坏，W15 抓出）
7. Tk 图片跨解释器（5 处 ImageTk.PhotoImage 补 master=self）
8. 引擎死亡路径漏清 guard（R5 统一销账的漏网，W19 抓出）
9. Tk root 创建闪烁（死 _default_root 残句柄 → create_tk_root 工厂，1/9→10/10）

### 四块判定/基建收口（新增模式只改这四处）
- `_foreground_busy()`：四模式前台独占谓词
- `_drill_occupied()`：drill 占用棋盘谓词（收口 8 处手抄）
- `_reset_engine_request_state()`：9 本 rid 挂账统一销账（引擎启/停/死三边界）
- `create_tk_root()`：root 创建健壮工厂（清死 default_root + gc + 退避重试）

### 测试基建
- test_ui_smoke 单 root 单例 + 删除重复定义的 test_drill_free_answer（死代码）
- ah.clean 清挂账字典；W6 finally 清队列挂账；run() 归一化 client/_training
- pyflakes 非测试文件 16→0；自审查 20 OK / 0 P0 / 0 P1

## 四、当前状态

- **全量 pytest：198 绿 + 2 外部错误，连续稳定**（test_ui_v6 缺 app
  fixture——并行会话未完成品，不碰）；**仿真 20/20**；对抗 144 PASS
- app.py ~10350 行；并行会话的 V6 shell/router 架构已激活（headless app
  带 shell，库窗口走内嵌页模式——测试断言库"关闭"要按 router.current 分支）
- user_settings.json：theme=dark

## 五、重要注意事项（新对话必读）

1. **并行会话协议**：修改前查文件 mtime（5 分钟内被外部改过→重读再改）；
   `analyzer/ui/` 目录、test_ui_v6.py、test_coach_ui.py、online_import.py
   等是并行 V6 开发区，未经用户要求不修改（读取/跑测试/写新场景可以）
2. **两大 bug 家族**（历史教训，docs/优化轮报.md 各轮有沉淀）：
   - "守卫链手抄漂移"——四模式条件手抄必漏（历史漏错题复习×6、finished×2）
   - "rid 账本撞号"——凡是**换客户端实例**的边界（引擎启停/死亡/换谱/场景
     切换）必须整本销账；真实客户端 rid 是确定性 q1,q2… 重启必撞
3. redraw 是同步合并模式；棋子 PIL 无反光（用户要求取消）
4. 批量队列在前台独占模式下按设计让路（暂停不损坏、退出自动续跑）
5. 点目×undo = 退出点目并悔棋（T-8 设计行为，非 bug）
6. 后台队列结果写在记录快照树上、**不动前台曲线**（设计语义，W18 校准过）

## 六、移交并行会话的事项（不碰其开发区，等它自行处理）

1. `test_ui_v6.py` 引用了不存在的 `app` fixture（全项目无 conftest.py）
2. `ui/render.py:59`、`ui/timeline.py:257` 有同款 PhotoImage 无 master 隐患
3. `test_coach_ui.py` 自建销 Tk root——建议改走 `ah.create_tk_root` +
   单例（闪烁已由 create_tk_root 兜底，但根治靠它单例化）

## 七、下一步建议（新对话可直接说）

- **"继续优化"** → R15（仿真轮，与 R14 增强轮交替）。候选语料方向：
  (1) 曲线窗口×高光区间/热图点击联动（W20 只覆盖了主曲线点击）；
  (2) 批量分析中途切谱/关窗的交错； (3) R14 的 P3 备忘（跨刷新增量
  评价缓存——仅当批量场景实测再变卡才值得）
- **"继续 UI 现代化"** → ui-modernizer 方向 4/5（79 tk.Label→CTkLabel、13 Toplevel→CTkToplevel）
- 或任何具体问题直接描述

## 八、文档索引

| 文档 | 内容 |
|---|---|
| docs/优化轮报.md | R2-R13 历轮完整记忆（发现/修复/经验沉淀/移交项） |
| docs/逻辑整改清单.md | 触发/逻辑修复全记录（v1/v2/v3） |
| docs/架构解耦与对抗检测.md | 图层/事件/模式管理器 + 对抗体系 |
| docs/UI重写总结.md | UI 现代化全记录 |
| docs/产品对标与整改清单.md | 涨棋网/星阵对标结论 |
