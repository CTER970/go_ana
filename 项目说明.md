# 文档维护提示（2026-07-10）

这是一份详版项目说明，保留了大量引擎、模型、源码和历史验证细节。若只是想启动项目、查入口或确认当前状态，请优先阅读：

- [README.md](README.md)
- [docs/文档索引.md](docs/文档索引.md)
- [analyzer/README.md](analyzer/README.md)

---

# KataGo 围棋引擎 · 项目说明文档

> 文档版本：2026-06-28（v3.38 问题手双分支深度对比版）｜ 引擎版本：**KataGo v1.16.5** ｜ 平台：Windows x64
> 本文档对 `d:\katago\` 下的**整个项目**做全面、详尽的说明，由三部分组成：① 官方开源源码 ② 开箱即用的运行环境（预编译引擎 + 已校验模型）③ 自研的图形分析器 `analyzer/`。所有事实均来自本地源码、官方 README/文档，以及对引擎与分析器的实际运行测试。

---

## 目录

1. [项目总览（TL;DR）](#1-项目总览tldr)
2. [目录结构详解](#2-目录结构详解)
3. [KataGo 是什么](#3-katago-是什么)
4. [引擎版本与计算后端](#4-引擎版本与计算后端)
5. [神经网络模型](#5-神经网络模型)
6. [引擎子命令（共 52 个）](#6-引擎子命令共-52-个)
7. [配置系统](#7-配置系统)
8. [快速上手](#8-快速上手)
9. [图形分析器 analyzer/（应用层）](#9-图形分析器-analyzer应用层)
10. [从源码编译](#10-从源码编译)
11. [自训练（Self-play）管线](#11-自训练self-play管线)
12. [常见问题与排错（FAQ）](#12-常见问题与排错faq)
13. [来源与校验](#13-来源与校验)
14. [附录 A：C++ 源码分层架构](#14-附录-ac-源码分层架构)
15. [附录 B：训练历史与研究方法亮点](#15-附录-b训练历史与研究方法亮点)

---

## 1. 项目总览（TL;DR）

本文件夹是一个**完整、可运行的 KataGo 工作区**，由三部分组成：

| 顶层目录 | 性质 | 内容 | 大小 |
|---|---|---|---|
| [KataGo/](KataGo/) | 开源源码项目 | `git clone` 自官方仓库 `lightvector/KataGo`，含完整 C++ 引擎源码、Python 训练脚本、文档、示例配置 | 79.5 MB |
| [katago-runtime/](katago-runtime/) | **开箱即用**运行环境 | 两个预编译引擎（CPU + GPU）、两个已校验模型、全部依赖 DLL、默认配置、中文说明 | 462 MB |
| [analyzer/](analyzer/) | **自研图形分析器**（应用层） | Python + tkinter：棋盘 UI + 棋局树 + 封装 `katago analysis` 子进程；自动棋局库；复盘三件套 + 稳健单局表现估计 + 自动文字复盘 + 三阶段表现分析 + 阶段训练 + 终局点目 | ~160 KB 代码 |

**三个可运行入口**：

| 想做什么 | 在哪 | 命令 |
|---|---|---|
| 打开图形界面下棋/复盘（最常用） | [analyzer/](analyzer/) | `python app.py` |
| 命令行 GTP 对弈/分析（引擎本体） | [katago-runtime/](katago-runtime/) | `.\katago-eigenavx2.exe gtp` |
| 无头跑分析器的自动化测试 | [analyzer/](analyzer/) | `python test_ipc.py` |

**已实测可运行**：
- **引擎**：CPU 版加载默认模型 `b18c384nbt`，于 9×9 棋盘生成棋步 `E5`，报告 `KataGo v1.16.5 / Eigen(CPU) backend / Model version 14`。
- **分析器**：核心测试覆盖坐标转换、提子/自杀/打劫规则、MoveTree 导航与分支、真实 KataGo analysis 通信、终局点目、复盘三件套、复盘报告、项目文件、自动棋局库、配置管理、SGF、Pass、热力图映射。空盘 19×19（chinese 规则、komi 7.5）首选 `Q16`、落子后正确轮到白方。

**30 秒上手（图形界面）**：
```powershell
cd d:\katago\analyzer
python app.py
```
打开窗口 → 点右侧【启动 KataGo】（OpenCL 首次运行会做 GPU 调优，可能几十秒到数分钟；之后加载会明显更快）→ 状态栏变绿「就绪」→ 在棋盘上点击落子，右侧自动显示 AI 推荐点。

**30 秒上手（纯命令行引擎）**：
```powershell
cd d:\katago\katago-runtime
.\katago-eigenavx2.exe gtp
```
即可进入 GTP 交互对弈（无需任何参数，因同目录已有 `default_gtp.cfg` 与 `default_model.bin.gz`）。

---

## 2. 目录结构详解

### 2.1 顶层布局

```
d:\katago\
├── KataGo\                  ← 开源源码项目（见 2.2）
├── katago-runtime\          ← 开箱即用运行环境（见 2.3）
├── analyzer\                ← 自研图形分析器（见 2.4 + 第 9 章）
└── 项目说明.md              ← 本文档
```

### 2.2 源码项目 [KataGo/](KataGo/)

官方仓库 `https://github.com/lightvector/KataGo.git` 的浅克隆。关键内容：

| 路径 | 说明 |
|---|---|
| [KataGo/README.md](KataGo/README.md) | 主 README：总览、GTP 用法、GUI 生态、模型选择 FAQ、后端、排错 |
| [KataGo/LICENSE](KataGo/LICENSE) | MIT 许可证（Copyright 2025 David J Wu） |
| [KataGo/Compiling.md](KataGo/Compiling.md) | 各平台编译指南（CMake、依赖、后端、分布式构建） |
| [KataGo/SelfplayTraining.md](KataGo/SelfplayTraining.md) | 自训练闭环（5 部件）说明 |
| [KataGo/TrainingHistory.md](KataGo/TrainingHistory.md) | 训练历史（g65 / g104 / g170 / 当前 kata1） |
| [KataGo/KataGoMethods.md](KataGo/KataGoMethods.md) | KataGo 在 AlphaZero 之外自创/改进的方法清单 |
| [KataGo/cpp/](KataGo/cpp/) | C++ 引擎源码（分层架构见附录 A） |
| [KataGo/cpp/main.cpp](KataGo/cpp/main.cpp) | 程序入口，子命令分发（52 个子命令） |
| [KataGo/cpp/command/](KataGo/cpp/command/) | 各子命令实现（gtp/analysis/benchmark/selfplay/...） |
| [KataGo/cpp/neuralnet/](KataGo/cpp/neuralnet/) | 神经网络后端（CUDA/TensorRT/OpenCL/Metal/Eigen/dummy） |
| [KataGo/cpp/configs/](KataGo/cpp/configs/) | 官方示例配置（含 `gtp_example.cfg`，与运行环境的 `default_gtp.cfg` 文本内容相同，仅行尾符不同） |
| [KataGo/python/](KataGo/python/) | Python 训练/洗牌/导出/分析脚本 |
| [KataGo/docs/](KataGo/docs/) | 深度文档（见 2.5） |

### 2.3 运行环境 [katago-runtime/](katago-runtime/)

| 文件 | 大小 | 说明 |
|---|---|---|
| `katago-eigenavx2.exe` | 4.5 MB | **CPU 引擎**（Eigen 后端，AVX2+FMA），任何 x64 CPU 可跑，保底选择 |
| `katago-opencl.exe` | 4.8 MB | **GPU 引擎**（OpenCL 后端），Nvidia/AMD/Intel 显卡均可，首次自动调优 |
| `default_model.bin.gz` | 93 MB | 默认模型 = `b18c384nbt`（自动加载） |
| `models\kata1-b18c384nbt-...bin.gz` | 93 MB | 轻量模型（与 default_model 字节相同） |
| `models\kata1-b28c512nbt-...bin.gz` | 259 MB | **最强模型**（2026-06-07 主训练线） |
| `default_gtp.cfg` | 34 KB | 默认 GTP 配置（自动加载） |
| `analysis_example.cfg` 等 5 个 | — | analysis / contribute / match / human5k / human9d 示例配置 |
| `libcrypto-3-x64.dll` / `libssl-3-x64.dll` / `libz.dll` / `libzip.dll` | — | **必须与 exe 同目录**（见 12.1） |
| `cacert.pem` | 217 KB | Mozilla 根证书束（HTTPS 用，如分布式训练） |
| `msvcp140*.dll` / `vcruntime140*.dll` | — | 捆绑的 MSVC 运行时（无需另装 VC++ Redistributable） |
| `README.txt` | 7 KB | 官方发行版自带说明 |
| `README-RUN.txt` | 4.5 KB | 本环境专用中文运行说明 |
| `gtp_logs\` | — | 运行日志输出目录 |

### 2.4 图形分析器 [analyzer/](analyzer/)

| 文件 | 行数 | 说明 |
|---|---|---|
| [analyzer/app.py](analyzer/app.py) | 2311 | **UI 层**（tkinter）：棋盘 Canvas + 右侧「总览 + AI/复盘/棋谱/导航标签页」；胜率条、AI 推荐点列表、轮询循环、终局点目模式、复盘三件套、三阶段摘要、阶段训练模式（我方设置/隐藏候选/提示/结束/重来/训练历史）、复盘报告、自动棋局库、**自动启动/快捷键/自动播放**；程序入口 |
| [analyzer/movetree.py](analyzer/movetree.py) | 190 | **棋局状态层**：`BoardState` 之上的 `MoveTree`/`MoveNode`——撤回/快进/分支/分析缓存；GTP 坐标转换；`score_result`/`double_pass` |
| [analyzer/board.py](analyzer/board.py) | ~119 | **棋盘逻辑层**：19×19 不可变盘面，提子、自杀检测、简单打劫 |
| [analyzer/katago_client.py](analyzer/katago_client.py) | ~121 | **引擎通信层**：启动 `katago analysis` 子进程，单行 JSON 查询/响应，后台线程按 id 分发 |
| [analyzer/config_manager.py](analyzer/config_manager.py) | ~130 | **配置管理**（v3.1）：自动定位 katago-runtime + `user_settings.json` 持久化 + 启动前检查 |
| [analyzer/analysis_guard.py](analyzer/analysis_guard.py) | ~45 | **异步请求守卫**（v3.5）：engine_session_id 隔离过期分析结果 |
| [analyzer/project_store.py](analyzer/project_store.py) | ~150 | **复盘项目文件**（v3.21）：`.kga.json` 保存/打开整棵棋局树、analysis 缓存、评论、点目结果、当前节点 |
| [analyzer/game_library.py](analyzer/game_library.py) | 378 | **本地棋局库**（v3.22→v3.27）：导入/收件箱扫描 SGF 自动保存原谱和项目快照，维护 `game_library/index.json`；支持去重、搜索、删除、最近打开、分析进度、我方设置、最差阶段训练题和训练记录；重复扫描不覆盖分析缓存 |
| [analyzer/heatmap.py](analyzer/heatmap.py) | ~40 | **热力图映射**（v3.6）：ownership/policy ↔ 棋盘坐标的纯逻辑（可无头测试） |
| [analyzer/sgf.py](analyzer/sgf.py) | 394 | **SGF 导入/导出**（v3.3/v3.7/v3.9）：MoveTree ↔ SGF（变化图 + C[]注释 + AI摘要 + 转义 + 让子 HA/AB/AW + **RE[]/点目摘要**） |
| [analyzer/score_estimator.py](analyzer/score_estimator.py) | 172 | **终局点目纯逻辑**（v3.9）：中国规则面积数法——死子移除、空区归属 flood fill、ownership 死子建议；不改原 BoardState |
| [analyzer/review.py](analyzer/review.py) | ~600 | **复盘评价纯逻辑**（v3.10→v3.31）：每手目损、胜率曲线、稳健单局表现、三阶段质量、自动中文棋局解说；只读 |
| [analyzer/review_report.py](analyzer/review_report.py) | ~220 | **Markdown 复盘报告**（v3.19→v3.32）：结论、单局表现、文字复盘、三阶段表现与有效问题棋 |
| [analyzer/training.py](analyzer/training.py) | 283 | **阶段训练纯逻辑**（v3.27）：按我执黑/白/双方和约 36 手滑动窗口寻找累计表现最差阶段，生成训练题；训练结束后按用户执棋手评价改善幅度、等级、好手、问题手和建议 |
| [analyzer/training_cache.py](analyzer/training_cache.py) | ~85 | **训练应手缓存**（v3.36）：用完整棋步生成跨重启稳定局面键，压缩 analysis，并按模型/规则/贴目/训练 visits/我方设置校验缓存 |
| [analyzer/branch_comparison.py](analyzer/branch_comparison.py) | ~145 | **问题手双分支比较**（v3.38）：统一为走子方视角，对比实战与 AI 分支的胜率、目差、稳定控制点、14 手主变并生成可追溯诊断 |
| [analyzer/move_quality.py](analyzer/move_quality.py) | 402 | **精细手段评价引擎**（v4.0）：6 级（最佳/好手/一般/不佳/恶手/未评价）+ 综合评分 + 置信度 + 胜负已定降级 + 原因文案 + 8 种问题标签 |
| [analyzer/player_profile.py](analyzer/player_profile.py) | 1504 | **长期个人画像**（v4.0）：按有效手数加权聚合多盘棋数据，拆分黑白/阶段/趋势/质量分布/问题标签/优弱点 |
| [analyzer/training_analysis.py](analyzer/training_analysis.py) | 724 | **训练后结构化分析**（v4.0）：精确局面对齐、重复错误/新错误/改善分类、训练评分与复习计划 |
| [analyzer/profile_store.py](analyzer/profile_store.py) | 539 | **画像缓存持久化**（v4.0）：JSON 原子写、来源指纹、增量更新、旧数据降级、从棋局库重建 |
| [analyzer/profile_report.py](analyzer/profile_report.py) | 183 | **个人分析 Markdown 报告**（v4.0）：核心指标、单局基线、阶段/黑白表现、逐盘趋势、建议与复习队列 |
| [analyzer/style_profile.py](analyzer/style_profile.py) | 328 | **8 维个人棋风画像**（v4.0）：本人视角、每百手频率、平均代价、趋势、置信度与代表局面 |
| [analyzer/style_cost.py](analyzer/style_cost.py) | 174 | **棋风成本分类**（v4.0）：可保留 / 继续观察 / 需要修正 / 样本不足 |
| [analyzer/growth_path.py](analyzer/growth_path.py) | ~120 | **成长路线**（v4.0）：一个主线目标、保留风格、修正习惯、观察项与代表局面 |
| [analyzer/style_report.py](analyzer/style_report.py) | ~110 | **棋风与成长路线 Markdown 报告**（v4.0） |
| [analyzer/style_store.py](analyzer/style_store.py) | ~90 | **棋风画像缓存持久化**（v4.0） |
| [analyzer/style_view.py](analyzer/style_view.py) | 185 | **独立棋风窗口**（v4.0）：维度表、成长路线、报告和复核队列 |
| [analyzer/deep_verification.py](analyzer/deep_verification.py) | 266 | **关键结论高 visits 复核队列**（v4.0）：原子持久化、稳定性汇总与结论降级 |
| [analyzer/mistake_book.py](analyzer/mistake_book.py) | 278 | **跨棋局错题本**（v4.0）：画像问题手同步、身份过滤、隐藏答案测验、间隔复习调度与原子持久化 |
| [analyzer/project_store.py](analyzer/project_store.py) | 158 | **复盘项目文件**（v3.21）：`.kga.json` 保存/打开——整棵棋局树 + analysis 缓存 + 评论 + 点目结果 + 当前节点 + RE + 规则/贴目 |
| [analyzer/game_library.py](analyzer/game_library.py) | 378 | **本地棋谱库**（v3.22→v3.27）：inbox 自动采集、sgf/projects/index.json、搜索/过滤/删除/打开/历史/收件箱扫描去重、我方设置、训练题索引和训练记录 |
| [analyzer/analysis.cfg](analyzer/analysis.cfg) | 30 | 分析配置（CPU/GPU 通用：1×8 线程，nnMaxBatchSize=16，maxVisits=200，黑方视角） |
| [analyzer/test_ipc.py](analyzer/test_ipc.py) | ~200 | 无头测试：规则 + 坐标 + 导航 + SGF + 真实 KataGo 通信闭环 |
| [analyzer/test_pass.py](analyzer/test_pass.py) | ~90 | Pass 真实节点测试（含真实引擎接受 pass） |
| [analyzer/test_sgf_variations.py](analyzer/test_sgf_variations.py) | 217 | SGF 变化图/注释/AI摘要/让子棋/点目 RE[]/C[] 测试（含引擎） |
| [analyzer/test_score_estimator.py](analyzer/test_score_estimator.py) | 166 | 点目纯逻辑测试：空盘/围空/死子/中立/贴目/ownership 建议/不改原盘/越界点串/短 ownership |
| [analyzer/test_review.py](analyzer/test_review.py) | 354 | 复盘评价纯逻辑测试：目损黑白视角翻转/未分析/pass/失误榜排序/序列/让子/只读/三阶段水平 |
| [analyzer/test_project_store.py](analyzer/test_project_store.py) | ~110 | 复盘项目文件测试：变化图/当前节点/analysis 缓存/点目结果 |
| [analyzer/test_game_library.py](analyzer/test_game_library.py) | 130 | 本地棋局库测试：自动入库/重复导入/项目快照更新/收件箱扫描/去重保留缓存/坏文件失败列表 |
| [analyzer/test_review_report.py](analyzer/test_review_report.py) | ~70 | Markdown 复盘报告测试：摘要、表现表、文字分析、三阶段、问题棋及无逐手表 |
| [analyzer/test_training.py](analyzer/test_training.py) | 87 | 阶段训练测试：最差阶段选择、按用户执棋方过滤、训练评价只统计用户手 |
| [analyzer/test_move_quality.py](analyzer/test_move_quality.py) | ~120 | 精细评价测试：排名口径、评分、置信度、原因、标签与序列化 |
| [analyzer/test_player_profile.py](analyzer/test_player_profile.py) | ~250 | 长期画像测试：加权聚合、身份、签名隔离、趋势与建议 |
| [analyzer/test_training_analysis.py](analyzer/test_training_analysis.py) | ~200 | 训练分析测试：局面对齐、分支模式、提示/重试扣分与复习计划 |
| [analyzer/test_profile_store.py](analyzer/test_profile_store.py) | ~90 | 画像缓存、指纹、原子写与损坏数据降级测试 |
| [analyzer/test_profile_report.py](analyzer/test_profile_report.py) | ~70 | 个人报告测试：基线、趋势、弱点建议、到期错题和口径说明 |
| [analyzer/test_style_profile.py](analyzer/test_style_profile.py) | ~100 | 棋风画像：8 维、本人过滤、样本边界、代表局面 |
| [analyzer/test_style_cost.py](analyzer/test_style_cost.py) | ~60 | 棋风成本与趋势优先级分类 |
| [analyzer/test_growth_path.py](analyzer/test_growth_path.py) | ~50 | 单一成长主线与数量约束 |
| [analyzer/test_style_report.py](analyzer/test_style_report.py) | ~60 | 棋风 Markdown 报告与复核章节 |
| [analyzer/test_deep_verification.py](analyzer/test_deep_verification.py) | ~70 | 复核队列、稳定性和成长路线降级 |
| [analyzer/test_mistake_book.py](analyzer/test_mistake_book.py) | ~90 | 错题本测试：同步、身份切换、作答判定、间隔调度、重分析与删除联动 |
| [analyzer/test_branch_comparison.py](analyzer/test_branch_comparison.py) | ~80 | 双分支比较测试：黑白视角、量化诊断和异步双请求汇总 |
| [analyzer/test_training_cache.py](analyzer/test_training_cache.py) | ~80 | 训练应手缓存测试：局面键、压缩、签名失效与写入 |
| [analyzer/test_training_controls.py](analyzer/test_training_controls.py) | ~70 | 全局提示与悔棋测试：训练整回合回滚、点目退出、非法候选过滤 |
| [analyzer/test_ui_smoke.py](analyzer/test_ui_smoke.py) | 285 | UI 综合冒烟（单 app 顺序段）：点目/复盘/易用性全断言（不启动引擎） |
| [analyzer/test_config_manager.py](analyzer/test_config_manager.py) | ~95 | 配置管理测试（自动定位/preflight/持久化/可移植/无硬编码） |
| [analyzer/test_analysis_request_guard.py](analyzer/test_analysis_request_guard.py) | ~75 | 异步请求守卫测试（session 隔离） |
| [analyzer/test_heatmap_mapping.py](analyzer/test_heatmap_mapping.py) | ~95 | 热力图映射测试（含真实引擎 180° 对称） |
| [analyzer/smoke_app.py](analyzer/smoke_app.py) | ~26 | UI 无头冒烟：构造窗口、落子/导航/重绘，不启动引擎 |
| [analyzer/README.md](analyzer/README.md) | — | 分析器自带说明（架构 + 用法） |
| `analysis_logs\` | — | analysis 引擎运行日志（`analysis.cfg` 的 `logDir`） |
| `__pycache__\` | — | Python 字节码缓存（cpython-311） |

### 2.5 文档目录 [KataGo/docs/](KataGo/docs/)

| 文件 | 一句话说明 |
|---|---|
| [Analysis_Engine.md](KataGo/docs/Analysis_Engine.md) | JSON 并行分析引擎协议（`katago analysis`），含 Human SL 分析指南 |
| [GTP_Extensions.md](KataGo/docs/GTP_Extensions.md) | KataGo 的 GTP 扩展命令（`kata-analyze`、规则设置、时间制等） |
| [GraphSearch.md](KataGo/docs/GraphSearch.md) | 蒙特卡洛**图搜索**（MCGS）技术长文，解释为何朴素图 MCTS 不成立 |
| [KataGoMethods.md](KataGo/KataGoMethods.md) | AlphaZero 之外的方法清单（含 Elo 影响） |
| [rules.html](KataGo/docs/rules.html) | 规则交互参考页（ko/计分/税/自杀/按钮等） |

---

## 3. KataGo 是什么

**定义**（引自 [KataGo/README.md](KataGo/README.md)）：

> *"KataGo remains one of the strongest open source Go bots available online. KataGo was trained using an AlphaZero-like process with many enhancements and improvements, and is capable of reaching top levels rapidly and entirely from scratch with no outside data, improving only via self-play."*

要点：
- **本质**：一个**围棋（Go）引擎**，实现 GTP（Go Text Protocol）文本协议，**本身没有图形界面**，需配合 GUI/分析软件使用（本项目 `analyzer/` 就是这样一个自研前端）。
- **作者**：David J Wu（GitHub ID `lightvector`）。
- **许可**：**MIT**（宽松，允许商用、修改、再分发）。作者特别声明该许可是个人授予，不代表其雇主（含 2020–2023 期间的 Facebook）。`cpp/external/` 下若干第三方库保留各自许可证。
- **训练方式**：类 AlphaZero 的**自我对弈（self-play）**强化学习，**不依赖任何外部棋谱**，从零开始。官方称：调优得当的话，单张顶级消费级 GPU 有望在几个月内从零训到超人类水平。
- **能力亮点**：
  - 不仅给出胜率，还**估算地盘与比分**（利于让子棋与复盘）；
  - 支持 7×7 到 19×19 多种棋盘；
  - 支持多种规则（中日韩古各派，见 [rules.html](KataGo/docs/rules.html)）；
  - 支持任意/整数贴目与让子；
  - JSON 批量分析引擎；
  - 公共分布式训练（`kata1`，[katagotraining.org](https://katagotraining.org/)）；
  - 模仿人类棋风的 Human-SL 模型。

---

## 4. 引擎版本与计算后端

### 4.1 版本

本环境引擎为 **KataGo v1.16.5**（`Version::getKataGoVersion()` 返回 `"1.16.5"`），发布于 **2026-05-25**，非预发布版。源码：[KataGo/cpp/main.cpp](KataGo/cpp/main.cpp)。

`katago version` 实测输出（CPU 版）：
```
KataGo v1.16.5
Git revision: ba938676d7f42d70950b3a535af2466fb642008c
Compile Time: May 25 2026 12:46:10
Using Eigen(CPU) backend
Compiled with AVX2 and FMA instructions
Compiled to support contributing to online distributed selfplay
```

> 分析器的 `test_ipc.py` 通过 `query_version` 取回的 `git_hash` 与上面一致（`ba938676...`），交叉印证。

### 4.2 计算后端（编译期选定，一对一）

KataGo 神经网络抽象在 [KataGo/cpp/neuralnet/nninterface.h](KataGo/cpp/neuralnet/nninterface.h) 之后，共有 **5 个真实后端 + 1 个 dummy**，在编译期由宏二选一（一个二进制 = 一个后端）：

| 后端 | 宏 | 目标 | 本项目是否提供 |
|---|---|---|---|
| OpenCL | `USE_OPENCL_BACKEND` | 跨厂商 GPU（AMD/Intel/Nvidia） | ✅ `katago-opencl.exe` |
| Eigen（CPU） | `USE_EIGEN_BACKEND` | 纯 CPU | ✅ `katago-eigenavx2.exe`（带 AVX2） |
| CUDA | `USE_CUDA_BACKEND` | Nvidia GPU（CUDA + cuDNN） | ❌（需自行下载/编译） |
| TensorRT | `USE_TENSORRT_BACKEND` | Nvidia GPU（最高吞吐） | ❌ |
| Metal | `USE_METAL_BACKEND` | Apple GPU（macOS/iOS） | ❌（非 Windows） |
| dummy | （无宏时） | 无计算，仅用于无 NN 的 CI/测试 | ❌ |

> **注意**：本仓库**没有 HIP/ROCm 后端**。AMD 显卡走 OpenCL 后端。

**后端选择建议**（引自 README）：
- 有任意还行 GPU → **OpenCL**（最易跑通，首次自动调优）；
- 有 Nvidia 显卡且追求极致 → **TensorRT**（通常优于 CUDA）；
- 无 GPU 或 GPU 太弱 → **Eigen + AVX2**（好 CPU 上 15–20 block 网可达 10–20 playouts/秒）。

---

## 5. 神经网络模型

### 5.1 命名规则

分布式网络的文件名严格遵循：`<run>-<arch>-s<step>-d<datastep>.bin.gz`，以默认模型为例逐段拆解：

```
kata1 - b18 c384 nbt - s9996604416 - d4316597426
  │      │   │   │     │              │
  │      │   │   │     │              └─ d = 训练数据位置（move）数，~43.2 亿
  │      │   │   │     └─ s = 全局自对弈训练步数（加权样本数），~100 亿
  │      │   │   └─ nbt = 架构标记 = "nanobytetile"（模型版本 14，当前架构）
  │      │   └─ c384 = 每个卷积层的通道数 = 384
  │      └─ b18 = 残差 block 数 = 18
  └─ kata1 = 训练 run 标识。kata1 是当前唯一、自 2019 年持续的分布式自对弈 run
```

**比较两个 kata1 网络的经验法则**：`b`/`c` 越大 → 越大越慢但越强；`s`/`d` 越高 → 训练越靠后。`nbt` 标记表示较新的嵌套瓶颈残差架构（model version 14）。

### 5.2 本项目包含的两个模型

| 模型 | 架构 | 大小 | 强度（分布式评级，相对） | 用途 |
|---|---|---|---|---|
| `kata1-b18c384nbt-s9996604416-d4316597426` | 18 block × 384 通道，nbt（v14） | 93 MB | Elo ~13623 | **默认**。轻量快速，CPU 友好，官方 README 推荐。已复制为 `default_model.bin.gz` |
| `kata1-b28c512nbt-s13255194368-d5935380940` | 28 block × 512 通道，nbt（v14） | 259 MB | Elo ~14108（约 +485） | **最强**。2026-06-07 主训练线最新网。每次评估更慢更吃算力，适合好 GPU 或多核 CPU |

> 强度数值是 KataGo 分布式训练站的**内部评级刻度**（非绝对 Elo），仅用于同 run 内横向比较。

**版本兼容**：这些 `nbt`（model version 14）网需要较新的 KataGo（嵌套瓶颈架构自 v1.13 引入）；本项目 **v1.16.5 原生支持**，已实测加载成功。

### 5.3 `.bin.gz` 文件格式

`.bin.gz` = **gzip 压缩的单个二进制**，内含：
1. **头部/元数据**：模型版本（此处 14）、模型内部名字串、架构参数（block 数、通道数、trunk/body 结构、policy/value head 形状、`nnXLen/nnYLen` 最大支持棋盘几何，此处 19×19）；
2. **训练好的权重**：所有卷积核、归一化层参数、policy/value head 权重（float32，可按运行时 `useFP16` 以 fp16/fp32 加载）。

`.gz` 仅是 gzip 外壳，引擎加载时流式解压。`version` 命令日志里的 `Model version 14` 和 `Model name: ...` 就是直接读自该头部——这也是旧引擎遇到更新的网能给出清晰版本错误的原因。格式定义见 [KataGo/cpp/neuralnet/](KataGo/cpp/neuralnet/)，**版本化**：新引擎能加载旧网，旧引擎不能加载新架构网。

### 5.4 去哪里获取更多模型

| 来源 | 地址 | 用途 |
|---|---|---|
| 官方训练站 | https://katagotraining.org/ | `kata1` run 最新/最强网（本项目两网即取自此） |
| 历史归档 | https://katagoarchive.org/ | 早期 run（pre-kata1）网、研究用网、轻量 `b10c128`/`b15c192` 扩展训练网 |
| Human-SL 模型 | KataGo v1.15.0 Release | `b18c384nbt-humanv0.bin.gz`，模仿人类棋风（按段位），需配合 `-human-model` 使用 |

Human-SL 用法示例（⚠️ 见下方前置条件）：

> **前置条件**：本项目**未内置** `b18c384nbt-humanv0.bin.gz`（只内置了配套配置 `gtp_human5k_example.cfg`）。需先从 [KataGo v1.15.0 Release](https://github.com/lightvector/KataGo/releases/tag/v1.15.0) 下载该网络文件并放入 `katago-runtime\` 目录后，下列命令才可运行。

```powershell
.\katago-eigenavx2.exe gtp -model models\kata1-b18c384nbt-s9996604416-d4316597426.bin.gz `
  -human-model b18c384nbt-humanv0.bin.gz -config gtp_human5k_example.cfg
```
（`gtp_human5k_example.cfg` 默认模仿 5 级棋手；可改 `humanSLProfile` 为 `preaz_5k/9d`、`rank_*`、`proyear_1800..2023` 等）

---

## 6. 引擎子命令（共 52 个）

KataGo 是单一 `katago` 二进制，第一个参数为子命令，在 [KataGo/cpp/main.cpp](KataGo/cpp/main.cpp) 的 `handleSubcommand()` 中分发到 [KataGo/cpp/command/](KataGo/cpp/command/) 下各 `MainCmds::*`。按用途分组：

### 6.1 常用 / 交互式（最常用）

| 子命令 | 说明 |
|---|---|
| **`gtp`** | 主 GTP 引擎，接入任意标准围棋 GUI 进行对弈/分析（[gtp.cpp](KataGo/cpp/command/gtp.cpp)） |
| **`benchmark`** | 用 GTP 配置测试不同线程数下的速度，给出 `numSearchThreads` 建议 |
| **`genconfig`** | 友好交互式生成并自动调优一份新 GTP 配置 |
| `analysis` | 并行 JSON 分析引擎，可批量分析整盘棋（[analysis.cpp](KataGo/cpp/command/analysis.cpp)）——**`analyzer/` 就是封装这个子命令** |
| `match` | 批量化让不同网络/搜索设置互相对弈（比 gtp 更高效） |
| `tuner` | （仅 OpenCL）为 GPU 做调优 |
| `contribute` | 连接 katagotraining.org 持续贡献自对弈棋局（需 `BUILD_DISTRIBUTED` 编译） |
| `version` | 打印版本/后端/编译信息 |

### 6.2 自训练相关

| 子命令 | 说明 |
|---|---|
| `selfplay` | 自对弈生成训练数据（[selfplay.cpp](KataGo/cpp/command/selfplay.cpp)） |
| `gatekeeper` | 轮询新网络，与当前最优网对弈筛选（[gatekeeper.cpp](KataGo/cpp/command/gatekeeper.cpp)） |

### 6.3 开局库工具（genbook.cpp / startposes.cpp）

`genbook`、`writebook`、`checkbook`、`booktoposes`、`comparebooks`、`findbookbottlenecks` —— 生成/校验/比较开局库。

### 6.4 起手式 / SGF 挖掘

`samplesgfs`、`dataminesgfs`、`trystartposes`、`viewstartposes`、`checksgfhintpolicy`、`genposesfromselfplayinit`、`writetrainingdata`、`evalsgf`（调试单个局面）。

### 6.5 GPU 测试 / 调优 / 诊断

`testgpuerror`（别名 `gputest`，测 FP16 vs FP32 误差）、`searchentropyanalysis`、`sampleinitializations`、`evalrandominits`。

### 6.6 测试 / 调试（runtests.cpp 等，约 25 个）

`runtests`、`runnnlayertests`、`runnnontinyboardtest`、`runnnsymmetriestest`、`runownershiptests`、`runoutputtests`、`runsearchtests(v3/v8/v9)`、`runselfplayinittests`、`runsekitrainwritetests`、`runnnonmanyposestest`、`runnnbatchingtest`、`runtinynntests`、`runnnevalcanarytests`、`runconfigtests`、`runbeginsearchspeedtest`、`runownershipspeedtest`、`runsleeptest`、`printclockinfo` 等。

### 6.7 其他

`sandbox`（开发用空入口）。

> **自动加载默认配置的子命令**（无需 `-config`/`-model`）：`gtp`、`benchmark`、`gputest`、`tuner`、`genconfig`。
> **必须显式 `-config`** 的：`analysis`、`match`、`contribute`、`selfplay`、`evalsgf`、`gatekeeper`、`genbook`、`startposes`、`writetrainingdata`。
> 注：`analyzer/` 通过 `katago_client.py` **显式**传 `-config analysis.cfg -model <b18网>`，不依赖自动加载。

---

## 7. 配置系统

### 7.1 自动加载约定（重要）

机制来自源码 [KataGo/cpp/command/commandline.cpp](KataGo/cpp/command/commandline.cpp)、[gtp.cpp](KataGo/cpp/command/gtp.cpp)、[KataGo/cpp/dataio/homedata.cpp](KataGo/cpp/dataio/homedata.cpp)：

- `defaultGtpConfigFileName()` 返回字面量 `"default_gtp.cfg"`（commandline.cpp:189）；
- `gtp` 子命令把它注册为隐式默认配置（gtp.cpp:1906），于是 `-config` 变成**可选**；
- 无 `-config` 时，`getConfigFiles()` 在默认目录里找第一个存在的 `default_gtp.cfg`；
- 模型同理：`getModelFile()` 依次找 `default_model.bin.gz`、`default_model.txt.gz`，第一个存在者胜出；
- **搜索目录**（`getDefaultFilesDirs()`）：
  - **Windows**：**仅** `katago.exe` 所在目录；
  - **Linux**：exe 目录，其次 `~/.katago`。

**结论**：本项目把 `katago-*.exe` + `default_gtp.cfg` + `default_model.bin.gz` 放在**同一目录** [katago-runtime/](katago-runtime/)，所以 `katago-eigenavx2.exe gtp` 无需任何参数即可运行（已实测）。

**临时覆盖**：`-override-config KEY=VALUE,KEY=VALUE` 可在不改文件的情况下覆盖配置项。

### 7.2 `default_gtp.cfg` 关键设置

`default_gtp.cfg` 与官方 [KataGo/cpp/configs/gtp_example.cfg](KataGo/cpp/configs/gtp_example.cfg) **文本内容相同**（仅行尾符不同：源码版 CRLF、运行版 LF），注释行即默认值。本项目实际启用的（非注释）关键项：

| 类别 | 选项 | 值 | 含义 |
|---|---|---|---|
| 规则 | `rules` | `tromp-taylor` | 命名规则集（可选 `chinese/japanese/korean/aga/new-zealand/...`） |
| 搜索强度 | `maxVisits` | `500` | 每步 MCTS 总访问数上限（**首要强度旋钮**，越大越强） |
| 搜索强度 | `numSearchThreads` | `6` | 搜索线程数（文件内注明「你大概想调这个！」，用 `benchmark` 调） |
| 认输 | `allowResignation` | `true` | 允许认输 |
| 认输 | `resignThreshold` | `-0.90` | 胜负效用低于此值且连续 `resignConsecTurns` 步则认输 |
| 认输 | `resignConsecTurns` | `3` | 连续多少步 |
| 日志 | `logDir` | `gtp_logs` | 每次运行写一个日志文件到此目录 |
| 日志 | `logAllGTPCommunication` | `true` | 记录每条 GTP 指令 |

### 7.3 常用配置项速查（均见 `default_gtp.cfg` 注释）

- **规则**：`koRule`(SIMPLE/POSITIONAL/SITUATIONAL)、`scoringRule`(AREA/TERRITORY)、`taxRule`(NONE/SEKI/ALL)、`multiStoneSuicideLegal`、`hasButton`、`whiteHandicapBonus`(0/N-1/N)、`ignoreGTPAndForceKomi`。
- **搜索强度**：`maxPlayouts`(新增 playout 上限)、`maxTime`(秒)、`numSearchThreads`、`ponderingEnabled`(对手回合是否思考)、`searchFactorWhenWinning`(领先时降速)。
- **GPU / NN**：`nnMaxBatchSize`、`nnCacheSizePowerOfTwo`(默认 20，缓存 2^N 个 NN 评估)、`nnRandomize`、`numNNServerThreadsPerModel`(多卡设为卡数)、`openclDeviceToUse`/`cudaDeviceToUse`、`openclUseFP16`/`cudaUseFP16`(auto)。
- **让子/强度调整**：`playoutDoublingAdvantage`(-3..3，负=变弱)、`dynamicPlayoutDoublingAdvantageCapPerOppLead`(动态让子补偿，大幅提升让子棋强度)。
- **分析**：`analysisPVLen`(15)、`reportAnalysisWinratesAs`(SIDETOMOVE，多数 GUI 期望此值)、`analysisWideRootNoise`(0.04，分析时加宽探索，求最强设 0)。
- **Eigen(CPU) 专属**：`numEigenThreadsPerModel`(默认同 `numSearchThreads`)。

### 7.4 示例配置文件

| 文件 | 用途 |
|---|---|
| `default_gtp.cfg` | 默认 GTP 配置（自动加载） |
| `analysis_example.cfg` | `katago analysis` 的模板（批量分析）——分析器的 `analysis.cfg` 即精简自此 |
| `contribute_example.cfg` | `katago contribute` 连接训练站贡献算力 |
| `match_example.cfg` | `katago match` 机器对弈池（展示按 bot 索引差异化参数） |
| `gtp_human5k_example.cfg` | 用 Human-SL 模型模仿 ~5 级人类（`humanSLChosenMoveProp=1.0`，几乎纯人类网） |
| `gtp_human9d_search_example.cfg` | 模仿 9 段并融合搜索（更强、稍失人味） |

> 另有 `gtp_example.cfg`、`task_example.cfg` 仅存在于源码 [KataGo/cpp/configs/](KataGo/cpp/configs/)，未随运行环境分发。

---

## 8. 快速上手

### 8.1 跑图形分析器（最常用的交互入口）

> **电脑新手**：无需命令行——双击项目根目录的 `run.bat` 即可打开；先双击一次 `create-shortcut.bat` 可在桌面生成快捷方式（详见 [`快速开始.txt`](快速开始.txt)）。下面是命令行等价方式。

```powershell
cd d:\katago\analyzer
python app.py
```

打开后**自动启动引擎**（v3.11，OpenCL 首次运行会做 GPU 调优）→ 鼠标悬停合法交叉点预览虚化棋子 → 点击落子 → 右侧「AI 推荐选点」显示 A/B/C 三个按钮及当前行棋方胜率；棋盘不叠加候选标记。

- **撤回 / 快进**：按钮 或 ←/→ 方向键　**分支**：撤回到某处另走一手即产生新分支
- **重新分析**：点【重新分析当前局面】　**清空**：回到空盘
- 勝率/目差均为**黑方视角**（见 `analysis.cfg` 的 `reportAnalysisWinratesAs = BLACK`）

> 详见 [第 9 章](#9-图形分析器-analyzer应用层)。

### 8.2 CPU 命令行对弈（保底，本机已验证）

```powershell
cd d:\katago\katago-runtime
.\katago-eigenavx2.exe gtp
```
进入 GTP 后输入：
```
boardsize 19        # 设置 19 路
clear_board         # 清空
genmove b           # KataGo 执黑走一步
play w q16          # 你执白下 Q16
showboard           # 显示棋盘
quit                # 退出
```
> 首次走棋含模型加载约 10 秒，之后每步很快。

### 8.3 切换最强模型 `b28c512nbt`

```powershell
.\katago-eigenavx2.exe gtp -model models\kata1-b28c512nbt-s13255194368-d5935380940.bin.gz
```

### 8.4 用 GPU 加速（需有 OpenCL 驱动）

```powershell
.\katago-opencl.exe gtp          # 对弈
.\katago-opencl.exe benchmark    # 基准测试 + 自动为显卡调优
```
首次运行会自动调优（几十秒到数分钟），之后缓存。

### 8.5 调优线程数

```powershell
.\katago-eigenavx2.exe benchmark
```
输出会建议一个 `numSearchThreads` 值，把它写进 `default_gtp.cfg`。

### 8.6 配合图形界面（GUI）

KataGo 无自带界面。推荐：
- **[KaTrain](https://github.com/sanderland/katrain)** —— 最易上手，自带 KataGo 一站式包，含分级弱化机器人；
- **[LizzieYzy Next](https://github.com/wimi321/lizzieyzy-next)** / [LizzieYzy](https://github.com/yzyray/lizzieyzy) —— Lizzie 的活跃分支，侧重实时复盘；
- **[q5Go](https://github.com/bernds/q5Go)**、**[Sabaki](https://sabaki.yichuanshen.de/)**、**[Ogatak](https://github.com/rooklift/ogatak)** —— 通用 SGF 编辑器/界面。

在 GUI 中把「引擎命令」指向本目录的 `katago-eigenavx2.exe`（或 `katago-opencl.exe`），工作目录设为本目录即可（因自动加载约定，无需 `-config`/`-model`）。

> 如果只想要「点棋盘下棋 + 看 AI 推荐」的最小体验，直接用本项目的 [analyzer/](analyzer/)，不必另装 GUI。

---

## 9. 图形分析器 analyzer/（应用层）

`analyzer/` 是本项目自研的**最小可用围棋分析前端**：打开窗口（**自动启动引擎**）→ 显示 19 路棋盘 → 点击落子 → 把当前局面发给本地 KataGo → 显示 AI 推荐点（胜率、目差、变化图）→ 可撤回/快进/分支（含**快捷键 + 自动播放**）→ **复盘三件套**（批量分析整盘 / 每手目损 / 胜率曲线）→ 生成复盘报告 → 自动沉淀进本地棋局库 → 终局点目（中国规则面积数法）。它**不是** KataGo 源码的一部分，而是一个独立的 Python 应用，通过子进程调用引擎。

### 9.1 三层架构

```
[app.py]            棋盘 UI（tkinter）：显示、点击落子、显示 AI 推荐
   │ 依赖
[movetree.py]       棋局状态层：BoardState + MoveTree（撤回/快进/分支/分析缓存）
   │ 依赖                 │ 依赖
[board.py] 棋盘逻辑      [katago_client.py]  与 `katago analysis` 进程通信（JSON over stdin/stdout）
```

设计原则：**三层完全解耦**——`board.py` 不依赖 UI 也不依赖引擎，`movetree.py` 只依赖 `board.py`，`katago_client.py` 只依赖标准库，`app.py` 把三者粘合起来。因此 `test_ipc.py` 可以**无头**测试规则与引擎通信，无需显示器。

### 9.2 运行

**图形界面**（需显示器）：
```powershell
cd d:\katago\analyzer
python app.py
```
打开后点【启动 KataGo】→ 就绪后点棋盘落子。

**无头测试**（命令行，不需显示器）：
```powershell
python test_score_estimator.py   # 点目纯逻辑（面积法/死子/中立/ownership 建议）—快，不启动引擎
python test_review.py            # 复盘评价纯逻辑（目损视角翻转/失误榜/序列）—快，不启动引擎
python test_ui_smoke.py          # UI 综合冒烟（点目/复盘/易用性，单 app）—不启动引擎
python test_ipc.py               # 规则+坐标+导航+真实 KataGo 通信闭环（约 10–30 秒）
python smoke_app.py              # UI 构造/落子/重绘冒烟，不启动引擎（快）
```

> ⚠️ **已知小问题**：在中文 Windows（GBK 控制台）下，`test_ipc.py` 最后一行 `print("全部通过 ✅")` 的 ✅ emoji 会触发 `UnicodeEncodeError`，导致脚本以非零退出码结束——但**此前所有断言均已通过、引擎也已正常关闭**，可视为通过。规避：`python -X utf8 test_ipc.py` 或 `$env:PYTHONUTF8=1` 后再跑，或先 `chcp 65001`。详见第 12.7 节。

### 9.3 线程模型与数据流

1. **主线程**（tkinter 事件循环）：响应点击/按钮 → 调 `MoveTree.play/undo/...` → 重绘棋盘 → 每 `POLL_MS`(80ms) 调 `_poll_loop()`。
2. **`_poll_loop()`**：从 `KataGoAnalysisClient.poll()` 取已就绪的 `(id, resp)`，按 id 找到挂账的 `MoveNode`，把分析结果缓存进节点并刷新 UI。
3. **引擎子进程**：`katago analysis -config analysis.cfg -model <b18网>`，工作目录 = `analyzer/`（故日志写进 `analysis_logs/`）。
4. **后台读线程**（client 内）：持续读引擎 stdout 单行 JSON，按 `id` 塞进队列；收到 `query_version` 响应后置 `ready=True`。
5. **后台 stderr 线程**（client 内）：排空引擎 stderr，避免缓冲区塞满导致进程阻塞；保留最近 300 行供排错。

> tkinter **非线程安全**，所以所有 UI 操作只在主线程做；后台线程只把结果放进队列，由主线程轮询消费。

### 9.4 `analysis.cfg` 关键设置

CPU/GPU 通用的精简配置（注释值=默认值，未列出的项由引擎内置默认填充）：

| 选项 | 值 | 含义 |
|---|---|---|
| `numAnalysisThreads` | `1` | 并行分析通道数（原型用 1） |
| `numSearchThreadsPerAnalysisThread` | `8` | 每个通道的 MCTS 搜索线程数 |
| `maxVisits` | `200` | 单局面 MCTS 总访问数（**首要强度旋钮**，调大如 800 分析更深但更慢） |
| `nnMaxBatchSize` | `16` | 神经网络批量大小；OpenCL 后端必需，缺失会导致 analysis 启动时报 `Could not find key 'nnMaxBatchSize'` |
| `reportAnalysisWinratesAs` | `BLACK` | 统一以黑方视角报告胜率/目差（UI 胜率条直观） |
| `analysisPVLen` | `12` | 主变（PV）长度 |
| `wideRootNoise` | `0.0` | 根节点加宽探索（0=最强；设小正数可看到更多候选） |
| `nnCacheSizePowerOfTwo` | `20` | NN 评估缓存 2^20 项（换局面时命中可加速） |
| `numEigenThreadsPerModel` | `8` | CPU(Eigen) 后端线程数 |
| `logDir` | `analysis_logs` | 引擎日志目录 |

### 9.5 切换引擎 / 模型 / 强度

点 UI「引擎/模型…」按钮打开配置窗口。窗口会自动扫描 `katago-runtime`，在下拉框列出可用的 `katago-*.exe` 与 `*.bin.gz`：

- **引擎**：默认优先 `katago-opencl.exe`（GPU/OpenCL 加速），也可切回 `katago-eigenavx2.exe`（CPU 保底）。
- **模型**：默认优先轻量 `b18c384nbt`，也可切到更强但更慢的 `b28c512nbt`。
- **规则/贴目/强度**：可设置规则、komi、复盘 `maxVisits`、训练速度、棋局库后台 visits；**复盘预设**一键切 `maxVisits`——`快 80`（更快较浅）/ `标准 200`（平衡）/ `深入 800`（更稳更慢）；训练速度独立为 `快速 80` / `均衡 140` / `精细 240`，用于阶段训练对弈、提示和训练评价补分析；点【检测配置】做启动前检查。
- **持久化**：点【应用】写入 `analyzer/user_settings.json`；若引擎或模型改变且当前引擎在运行，会自动停止旧 analysis 进程并重启。

调强：把窗口里的复盘 `maxVisits` 调大（如 800）分析更深但更慢；训练要保持手感则用「快速」或「均衡」。OpenCL 启动失败时切回 CPU 版；需要更细的 CPU 调度时，再改 `analysis.cfg` 的 `numSearchThreadsPerAnalysisThread` / `numEigenThreadsPerModel`。

### 9.6 候选点 / 主变 10 步 / 前三选

**AI 推荐选点**（v3.33）：右侧独立模块固定显示三个按钮（按 KataGo 的 `order` 排序，A 为首选）：
```
A  Q16  黑53.2%  目差 +1.8
     Q16→D4→Q3→R16→C4
```
- 胜率/目差均为**黑方视角**（`reportAnalysisWinratesAs = BLACK`）
- 每行由 `A/B/C + 坐标` 按钮和当前行棋方胜率组成；白方行棋时自动把黑胜率换算为白胜率
- 棋盘不再绘制候选圆圈，避免遮挡棋形；单击按钮查看 PV，双击按钮落子

**主变 10 步**（v3.17）：点【主变 10 步】显示首选 `pv[:10]` 的后续 10 步；棋盘只在用户主动开启主变时绘制带圈数字，关闭后恢复干净实战局面。

**前三选切换**（v3.18/v3.33）：主变模式下点 A/B/C 推荐按钮切换对应主变；`_pv_candidates` 与过滤后的按钮顺序一致，自动跳过 pass/无效坐标。

### 9.7 复盘（批量分析 / 每手目损 / 评价分级 / 胜率曲线）

围绕复盘核心问题——「这盘哪几手下错了、错多严重、形势在哪手逆转」——把分析器升级为「**复盘教练**」。纯逻辑在 [review.py](analyzer/review.py)（只读 tree/analysis，可无头测试，见 [§9.11.6](#9116-reviewpy--复盘评价纯逻辑)）。

**【分析整盘】批量分析**：把主线（根 → 主线末尾）全部尚无 analysis 且无 pending 的节点一次性发给 KataGo（`numAnalysisThreads` 内部排队、`analysis_guard` 防污染）；状态栏显示进度 `已分析 X / N`，完成后沿主线导航**零等待**（每节点命中缓存）。批量进行中可正常导航/落子，互不干扰。

**每手目损（blunder detection）**：对每一手 N（父 P）：

```
best     = P.analysis.moveInfos 按 order 排序的 [0]
S_best   = best.scoreLead                  （黑视角，走 best 之后）
S_actual = N.analysis.rootInfo.scoreLead   （黑视角，走 actual 之后）
黑走：loss = S_best − S_actual
白走：loss = S_actual − S_best             （走子方视角翻号）
loss = max(0, loss)                         （≥0；大 = 失误大）
```

视角：`reportAnalysisWinratesAs = BLACK` 下 scoreLead 恒为黑视角；loss 取**走子方**视角，故白走翻一次号。P/N 任一缺 analysis、或 P 无 moveInfos → `analyzed = False`、`loss = None`。

**底层评价分级**（v3.19）：内部仍按目损给每手分级，供阶段统计和问题筛选使用：

| 目损（目） | 等级 | 棋盘色圈 |
|---|---|---|
| < 1.0 | 好 | 绿 #1a8a4c |
| 1.0 – 3.0 | 普通 | 不标 |
| 3.0 – 6.0 | 疑问 | 橙 #e8a317 |
| ≥ 6.0 | 恶 | 红 #d23a3a |

v3.32 不再向用户展示全盘逐手评价。复盘页改成五个有边界的功能框：复盘工具、阶段训练、阶段概览、对局分析、问题棋分析。对局分析使用带滚动条的只读文本；问题棋表只列仍有胜负意义且造成明显目损或胜率下降的手，显示实战、目损、胜率损失和 AI 建议。

**三阶段表现分析**（v3.26/v3.31）：自动把主线划分为**布局 / 中盘 / 关子**三个阶段，并给出平均目损、表现质量、好/普通/疑问/恶手数量、代表好手和重点问题手。阶段质量使用“优秀/稳定/有波动/问题较多”，不再把双方混合目损映射成段位。

- 阶段划分在 [review.py](analyzer/review.py) 中统一实现：短局按主线总手数三等分；长局对布局阶段做上限约束，避免 200 手棋的「布局」被拉得过长。
- `ReviewReport.phase_summary()` 返回三阶段统计；`phase_stats("布局"|"中盘"|"关子")` 可单独取某阶段。
- UI 的复盘标签页会显示紧凑摘要，如 `布局 优秀/0.4 | 中盘 问题较多/4.0 | 关子 稳定/1.0`。
- Markdown 报告逐阶段列出手数范围、已评手数、平均目损、表现质量、好/普通/疑问/恶、代表好手、重点问题和阶段评价。

**阶段训练模式**（v3.27）：把「问题阶段」从复盘结论变成可重下的训练题。训练题不是抓最大单手失误，而是在已分析主线上用约 36 手滑动窗口寻找累计表现最差的阶段，综合累计目损、胜率损失、疑问/恶手数量和连续问题手；支持按「我执黑 / 我执白 / 双方」过滤，避免把对手的问题阶段当成你的训练题。

- 导入 SGF 入库后，UI 会尝试自动启动 KataGo 并分析整盘；批量分析完成后，`game_library.index.json` 自动写入 `trainingTask`。棋局库打开且引擎就绪时，后台队列会为尚无训练题的离线棋局补主线分析并写回 `trainingTask`，不切走当前棋盘；如果训练题尚未生成，点【开始阶段训练】仍会自动启动/等待引擎、分析整盘、生成训练题，完成后直接进入训练。
- 棋谱库列表包含「我方 / 训练题 / 快速训练准备 / 最近训练」列；点【我执黑】/【我执白】/【双方】可改变训练题统计口径，点【训练历史】查看最近训练记录。
- 点【开始阶段训练】会打开该局并跳到问题阶段开始前一手；若起点不是你的回合，AI 会先补到你的回合。
- 训练中隐藏 AI 候选点，避免直接看到答案；系统不主动纠错、不打断思考，只在用户主动点【提示】时查看首选，或【结束训练】/【重来】/【原实战】控制训练。
- 训练题生成后，后台会沿阶段最多预演 20 个回合；每回合计算用户前三选、AI 应手和应手后局面，压缩写入 `game_library/training_cache/<record-id>.json`。这不是修改神经网络权重，而是建立该棋局专属的持久化应手树。
- 进入训练优先读取磁盘应手缓存，因此已覆盖变化不要求 KataGo 保持开启；用户走入未覆盖分支时才静默启动引擎补算，并把新局面追加回缓存。模型、规则、贴目、训练 visits 或我方设置改变后缓存自动失效重建。
- 结束后自动补齐分析并生成训练评价：只统计用户执棋手，对比原问题阶段同方平均目损，输出 A/B/C/D、改善幅度、好/普通/疑问/恶统计、训练中的好手/重点问题手和下一轮建议。
- 训练变化作为棋树分支随 `.kga.json` 项目快照保存；训练评价追加到棋局库索引的 `trainingSessions`。

**单局表现估计**：排除胜负已定局面并限制单手异常值后，分别估计黑白本盘表现，详见 [§9.8](#98-对局评分标准参考棋力档)。

**【胜率曲线】**：弹出 Toplevel，Canvas 画黑视角 `scoreLead` 折线（0 目为中线，上 = 黑好 / 下 = 白好），疑问/恶手点叠红、当前手黄色竖线；**点击曲线**跳到最近的那一手。批量分析回流时实时刷新。

### 9.8 对局评分标准（参考棋力档）

v3.31 将旧的“全盘平均目损直接映射段位”替换为 `ReviewReport.player_performance(color)`：

```text
单局表现估计（非官方段位）
黑 业余6段表现（5段-7段+）｜稳健 0.31 · 有效 35/65
白 业余5段表现（4段-6段）｜稳健 0.58 · 有效 36/64
```

- 只统计落子前该方胜率在 `2%-98%` 的局面；胜负已定后的目差波动不参与。
- 每手计入的目损上限为 `3.0`，降低一次战术崩盘对均值的支配。
- 有效样本少于 8 手不估段位；按样本量显示估计区间和“较低/有限/中等”可信度。
- 交叉校准：库内两盘 `cter` 对野狐精灵官方 5 段的 129/247 手棋，白方稳健目损分别为 `0.58/1.04`，均归入 5 段表现；目标盘黑方 `0.31` 为 6 段表现。
- 结论只代表单局竞技表现，不是在线账号段位或长期棋力认证；visits 越高越稳定。

复盘页新增 `game_commentary()` 自动文字分析：说明胜负结果、布局/中盘/关子走势、关键胜率反转和有意义的复盘重点。SGF 的 `PB/PW` 姓名随 `.kga.json` 保存，旧棋谱库项目打开时从原 SGF 兼容补回。

### 9.9 交互与视觉

**自动启动**（v3.11）：打开软件即后台拉起 KataGo（`_maybe_autostart` → `_start_katago(quiet=True)`），加载中可落子、就绪后自动补分析；失败安静跳过（状态栏提示，不弹窗）。手动【启动/停止】按钮仍可用。

**快捷键 + 自动播放**（v3.11）：

| 键 | 动作 |
|---|---|
| ← / → | 单步撤回 / 快进 |
| Home / End | 跳到第一手 / 主线末尾 |
| Space | 自动播放 / 暂停 |
| PgUp / PgDn | 一次翻 10 手 |
| **F11** | **全屏切换** |
| **Esc** | **退出全屏** |

【▶ 播放】按钮或 Space 自动播放主线（每 `PLAY_MS`=800ms 一步），到末尾自停、任何手动操作（落子/导航/Pass）自停。

**棋局进度条**（v3.14）：导航区 ttk.Scale，拖动/点击跳主线第 N 手；导航/落子/播放后同步（`_update_scale`，`_scale_suppress` 防反馈循环拽回主线）；旁显 `N/M`。

**形势判断**：胜率条 + 大字 `形势：黑/白 +X.X 目`（`scoreLead`）+ `领地：黑 X · 白 Y`（按 ownership 统计 |v|>0.05 的点数）。点【热力图】切「地盘」显示 ownership 双色归属：黑属地点用黑色交叉点标记、白属地点用白色带边框标记，点径按归属强度分档，避免旧版整格 stipple 热力图过糊。

**界面与视觉**（v3.15/v3.16）：清爽浅色调色板（`COLORS`：面板 #f5f6f8 / 棋盘 #e8b96b / 主色蓝 #1a5fd0）+ ttk clam 主题；右侧分 **5 张 LabelFrame 卡片**（对弈 / AI 分析 / 形势 / 复盘 / 棋谱）；棋盘**落影边框**（外层阴影色 Frame + 内棕边框）+ 棋子 **3 层高光球感**（黑子双层亮高光、白子球阴影+左上高光）+ 星位**双圈** + 坐标等宽 Consolas。

**全屏自适应缩放**（v3.27）：`resizable(True, True)` 解锁窗口缩放；`<Configure>` 事件 → `_on_configure` 防抖（80ms `after`）→ `_do_resize` 按窗口高度重算 `CELL/MARGIN/BOARD_PIX`（CELL 范围 12–50px）+ `canvas.config(width, height)` 同步 + `redraw`——棋盘/棋子/星位/坐标/候选/pv/三角标/评价圈全部随窗口缩放。**F11 全屏**（`attributes('-fullscreen', True)`）/ **ESC 退出**。太小提示（avail < 200 不调）。

### 9.10 终局点目（中国规则 / 面积数法）

终局阶段点【点目（终局数棋）】进入点目模式：以当前局面计算地盘、标记死子、按面积法判胜负，确认后把结果写入 SGF。第一版**只支持中国规则**，不做日本规则精确点目、复杂双活、提子计税（见 [§9.14](#914-路线v3-稳定复盘版--终局点目--复盘三件套--易用性--持久化已完成)「仍可扩展」）。

**面积数法**（[score_estimator.py](analyzer/score_estimator.py)）：

```
黑方面积 = 黑活子数 + 黑属地空点数
白方面积 = 白活子数 + 白属地空点数 + 贴目
margin  = 黑方面积 − 白方面积（正=黑胜，负=白胜，0=和棋）
```

空区归属用 flood fill：一块连续空区域只邻黑→黑地，只邻白→白地，同时邻黑白或不邻任何棋→中立（不计入任一方）。

**点目模式交互**：

| 操作 | 效果 |
|---|---|
| 点【点目（终局数棋）】 | 进入/退出点目模式；以当前节点盘面建 `ScoreEstimator` |
| 点击棋子 | 标记/取消死子（红色 X 叠加）；计分时该子视作从盘面移除 |
| 点击空点 | 不落子，状态栏显示该点归属（黑地/白地/中立） |
| 【AI 死子建议】 | 按 KataGo ownership 建议（黑子强偏白 / 白子强偏黑，阈值 0.75）合并进死子集 |
| 【确认结果】 | 锁定点目写回 `tree.score_result`，自动退出点目 |

- **地盘图**：黑地=深色大点、白地=浅色大点并加边框、中立=黄点（点目模式下替代普通模式的候选点与热力图），比旧版 3px 小点更容易看清。
- **实时刷新**：每次标记/取消死子，右侧面板立即重算（黑/白活子、地、贴目、合计、胜负、死子清单）。
- **双 Pass 提示**：连续两手 pass 时弹窗询问是否进入点目（不强制；同一节点只问一次）。
- **模式隔离**：点目模式下禁止一切修改 MoveTree 的操作——落子/撤回/快进/清空/Pass/分支←→/树视图跳转/导入 SGF——避免死子标记与盘面失同步。
- **不动原盘**：死子只在 `ScoreEstimator` 内部的临时 `scoring_grid` 移除，`BoardState` 与 `MoveTree` 原对象**永不修改**。

**ownership 死子建议**（进入点目时若当前节点已有 ownership，弹窗询问是否采用）：

```
ownership > 0 → 偏黑属地；  ownership < 0 → 偏白属地
黑棋所在点 ownership < −0.75 → 建议判死（黑死子）
白棋所在点 ownership > +0.75 → 建议判死（白死子）
```

仅作建议，最终死子集合以用户手动确认/标记为准。

**SGF 写入**（确认后【导出 SGF】，[sgf.py](analyzer/sgf.py)）：

- `RE[]`：写根属性，如 `RE[B+3.5]` / `RE[W+7.5]` / `RE[0]`（确认结果**覆盖**原 RE）。
- 根 `C[]`：追加点目摘要（规则 / 黑白明细 / 结果 / 死子清单），**不覆盖**既有根注释，换行原样保留。
- 未确认结果但导入的原 SGF 已有 `RE[]`，导出时**保留原 RE[]**（不写点目摘要）。

### 9.11 模块逐个详解

#### 9.11.1 [board.py](analyzer/board.py) — 棋盘逻辑层
- 常量 `EMPTY/BLACK/WHITE = 0/1/2`；坐标 `grid[y][x]`，`y=0` 为顶部、`x=0` 为左侧。
- `BoardState` 用 `__slots__`，**不可变快照**：`try_play(x,y)` 返回一个**新的** `BoardState`，原盘不变（供 `MoveTree` 每节点存一份）。
- 落子三步判定：① 提对方无气棋串（BFS 求棋串与气）→ ② 自杀检测（提子后本串仍无气=非法）→ ③ 简单打劫（新盘面与上一手落子前的 `prev_grid` 完全相同=非法）。
- 记录 `captures`（各方累计提子数）、`last_move`、`to_move`。
- 与 UI、KataGo 完全解耦；GTP 坐标转换在 `movetree.py` 完成。

#### 9.11.2 [movetree.py](analyzer/movetree.py) — 棋局状态层
- `COLS = "ABCDEFGHJKLMNOPQRST"`（**跳过 I**，符合 GTP 约定）。
- `xy_to_point(x,y)` ↔ `point_to_xy("Q16")`：GTP 列=A..T，GTP 行 1=底部，故 GTP 行 = `size - y`。
- `MoveNode`：每个节点存 ① 落子后的盘面快照 `board` ② `move=(色字母,(x,y))`（根节点为 None）③ `parent`/`children`（首个子节点=主线）④ `analysis`（缓存的 KataGo 响应 dict）⑤ `comment`（SGF C[] 注释）。
- `MoveTree`：`play`（命中既有分支则进入，否则新建）、`undo`（回父）、`redo`（进主线第一子）、`reset`（回根）、`siblings`/`sibling_index`/`goto_sibling(±1)`（兄弟分支循环切换，供 UI「分支←/→」与树视图）。
- `moves_list()`：从根到当前节点的落子序列，格式 `[["B","Q16"],...]`，**直接作为 KataGo analysis 查询的 `moves` 字段**。
- **分析缓存**：每个节点缓存一次分析结果，回到旧局面无需重算。
- **终局点目**（v3.9）：`score_result`（确认后的 `ScoreResult`，供 SGF 导出写 RE[]）+ `double_pass` 属性（当前节点与上一手均为 pass，供点目提示）。

#### 9.11.3 [katago_client.py](analyzer/katago_client.py) — 引擎通信层
- 协议见 [KataGo/docs/Analysis_Engine.md](KataGo/docs/Analysis_Engine.md)：启动 `katago analysis -config <cfg> -model <net>`，stdin 写单行 JSON 查询、stdout 读单行 JSON 响应（异步、按 `id` 匹配）。
- `start()`：Popen 子进程 → 起两个守护线程（读 stdout / 排空 stderr）→ 发 `query_version` 探测就绪，收到响应置 `ready=True`。
- `analyze(query)`：给查询分配自增 `id`（`q1`,`q2`,...），返回该 id；结果稍后经 `poll()` 取回。
- `poll()`：非阻塞取回所有已就绪的 `(id, resp)`。
- `recent_stderr(n)`：取最近 n 行引擎 stderr（排错用）。
- `is_alive()` / `stop()`：进程存活判断 / 关闭 stdin + `terminate()`。
- 线程安全：`_send_lock` 保护 stdin 写入；结果经队列传递（tkinter 非线程安全，UI 仅在主线程动）。

#### 9.11.4 [app.py](analyzer/app.py) — UI 层（tkinter）
- `GoAnalyzer(tk.Tk)`：左侧木色棋盘 Canvas（30px 格、26px 边距，含星位与坐标），右侧为固定「总览」+ `ttk.Notebook` 标签页（AI / 复盘 / 棋谱 / 导航）。
- **按钮**：启动/停止 KataGo、上一步/下一步、**▶ 播放**、清空、Pass、重新分析当前局面、**分析整盘（批量）**、导出/导入 SGF、**打开/保存项目**、**棋谱库**、**开始阶段训练**、**提示/结束训练/重来/原实战**、**训练历史**、**点目（终局数棋）**、**胜率曲线**、**导出报告**、热力图（关/地盘/策略）、分支←/→、树视图、引擎/模型…。
- **全局提示与悔棋**（v3.37）：棋盘下方常驻【提示 `F1`】和【悔棋 `Ctrl+Z`】。提示在棋盘以绿色虚线圈标出 AI 首选，缺分析时等待回流后自动显示；训练中只响应用户主动请求。普通悔棋退一手，训练悔棋撤销最近用户手及其后的 AI 应手并同步清理挂起请求，点目悔棋先退出点目再退一手。
- **问题手双分支深度对比**（v3.38）：整盘分析后自动排队最严重的 3 手，每次以相同 `300-800 visits` 并行重算实战落点与 AI 首选。问题棋模块提供【对比结论 / 实战变化 / AI变化】，显示走子方胜率、局面价值、稳定控制点与 14 手主变；切换分支时棋盘回到问题手之前并显示编号变化。
- **胜率条**：按黑方胜率填充黑白两段。
- **AI 推荐选点模块**：三个固定 `ttk.Button` + 胜率标签；`TOP_N=3`，单击显示 PV，双击落子，棋盘不绘制候选点。
- **合法落点悬停**（v3.34）：Canvas `<Motion>/<Leave>` 驱动 `_hover_point`；用 `BoardState.try_play()` 只读验证占位/自杀/打劫，合法时以 stipple 网点、虚线轮廓绘制当前方虚子；落子、导航、点目、主变、缩放和训练 AI 回合自动清除。
- 落子 → `_after_navigate()` → 重绘 + 若当前节点无缓存分析则 `_request_analysis()`；`_poll_loop()` 每 80ms 取结果回填。
- **易用性**（v3.11）：启动时 `_maybe_autostart()` 后台拉起引擎（quiet，失败仅状态栏提示，加载中可落子、就绪后自动补分析）；快捷键 `←/→`(单步) `Home/End`(首尾) `Space`(播放) `PgUp/PgDn`(翻 10 手)；`_start_auto_play`/`_auto_play_step`/`_stop_auto_play` 每 `PLAY_MS`(800ms) 沿主线 redo，到末尾自停，任何手动操作（落子/导航/跳转/Pass）经 `_stop_auto_play` 自停。
- **终局点目模式**（v3.9）：`enter_scoring`/`exit_scoring` 进出；点目模式下面板显示死子/地盘/结果，点击棋子 `_on_scoring_click` 切换死子并 `_refresh_scoring` 实时重算；`_block_in_scoring` 拦截一切改 MoveTree 的操作；双 pass 经 `_check_double_pass` 弹窗；`confirm_score` 写回 `tree.score_result`。详见 [§9.10](#910-终局点目中国规则--面积数法)。
- **复盘分析**（v3.10→v3.35）：`analyze_mainline` 批量分析主线；`_update_review_state` 刷新问题棋、文字分析、阶段概览、棋力表和曲线；`meaningful_problems()` 排除胜负已定局面并按胜率影响排序；`bad_move_intent()` 只为目损 ≥ 6 的恶手生成实战与 AI 选点意图对比。
- **阶段训练模式**（v3.30→v3.36）：默认引擎使用 GPU/OpenCL；导入入库后自动完成主线分析和 `trainingTask`。随后 `_start_training_cache_background()` 从训练起点沿 AI 首选主线推进最多 20 回合，每个用户回合展开前三选，并预分析 AI 应手及应手后局面；结果由 [training_cache.py](analyzer/training_cache.py) 压缩后单独持久化。`_start_stage_training()` 载入匹配当前模型/规则/贴目/visits/我方设置的缓存，用户落子和 AI 应手均先查缓存；未命中才由 `_request_analysis(training=True)` 静默启动 KataGo 兜底，新响应继续写回缓存。训练中隐藏候选、不主动纠错，结束后统一评价。
- 关闭窗口自动 `_close_graph()` + `_stop_auto_play()` + `_stop_katago()` 停掉引擎子进程与定时器。

#### 9.11.5 [score_estimator.py](analyzer/score_estimator.py) — 终局点目纯逻辑（v3.9）
- **`ScoreResult`**（dataclass）：黑/白活子、黑/白地、中立点、贴目、黑/白面积、margin、winner(`B`/`W`/`Draw`)、`result_text`（SGF RE[]，如 `B+3.5`/`W+7.5`/`0`）、死子清单、各类归属点列表。
- **`ScoreEstimator(board, komi)`**：`set_dead_stones`/`toggle_dead_stone`/`get_dead_stones` 管理死子；`compute_chinese_area_score()` 算面积法结果。
- **死子处理**：`_scoring_grid()` 复制盘面并移除死子（越界/非法点串安全跳过），**不改原 `board`**；死子按原色分类计入 `dead_black`/`dead_white`。
- **空区归属**：flood fill 每块连续空区域，按相邻棋子色判定黑地/白地/中立。
- **`suggest_dead_stones_from_ownership(ownership, threshold=0.75)`**：ownership 短/残缺时安全跳过；返回建议死子集（仅建议，不自动确认）。
- 与 tkinter / KataGo 完全解耦，可被 `test_score_estimator.py` **无头**测试。

#### 9.11.6 [review.py](analyzer/review.py) — 复盘评价纯逻辑（v3.10→v3.26）
- **`MoveEvaluation`**（dataclass）：`node_nid`/`move_number`/`color`/`coord`/`is_pass`、`score_lead_before|after`、`winrate_before|after`（均黑视角）、`best_move`/`best_score_lead`、`loss`（走子方视角目损，≥0；None=无法评价）、`analyzed`、`agreement_rank`（actual 在父 moveInfos 前 5 选排名，0=首选；None=不在/未分析，v3.20）。
- **`ReviewReport(tree)`**：`meaningful_problems()` 生成有效问题棋；`player_performance(color)` 生成稳健单局表现；`game_commentary(PB,PW)` 生成中文对局解说；`phase_stats/phase_summary` 给出三阶段质量评价。
- **模块级函数/常量**：`grade_of(loss)`（好<1/普通 1–3/疑问 3–6/恶≥6，v3.19）、`rank_of(avg_loss)`/`rank_short(avg_loss)`（平均目损→业余段级位，v3.20）、`normalize_phase(phase)`（支持 opening/middle/endgame 与 布局/中盘/关子，v3.26）+ 常量 `PHASES/PHASE_LABELS/GRADE_* / RANK_BANDS`。
- **loss 视角**：黑走 `S_best − S_actual`，白走 `S_actual − S_best`，`max(0,·)`；P/N 缺 analysis 或 P 无 moveInfos → `analyzed=False`。
- **只读**：不修改传入的 tree 或 analysis dict；与 tkinter/KataGo 进程解耦，可被 `test_review.py` **无头**测试。

#### 9.11.7 [project_store.py](analyzer/project_store.py) — 复盘项目文件（v3.21）
- **`.kga.json`** 格式：序列化整棵 `MoveTree`（含变化图/分支）+ 每个 `MoveNode.analysis` 缓存 + `comment` + `tree.score_result`（点目结果）+ `_sgf_re`（原 RE）+ 当前节点 nid + 规则/贴目。
- `save_project(tree, path)` / `load_project(path) -> tree`：JSON 往返；恢复后导航命中缓存零等待、点目/RE 保留。
- 与 tkinter/KataGo 解耦，可被 `test_project_store.py` **无头**测试（变化图/当前节点/缓存/点目结果往返）。

#### 9.11.8 [game_library.py](analyzer/game_library.py) — 本地棋谱库（v3.22）
- **库结构**：`game_library/{inbox, sgf, projects, training_cache, index.json}`；inbox 自动扫描 → 去重 → 入库（存原 SGF + 项目快照）。
- `GameLibrary`：搜索/过滤（名/规则/路径）、列表元数据（手数/已分析节点数/规则/贴目/最近时间）、打开/删除/历史、收件箱扫描。
- 分析回流自动更新库内项目快照（减少下次重复分析）；主线分析足够时按 `playerColor` 自动生成/刷新 `trainingTask` 和 `trainingSummary`；训练结束后追加 `trainingSessions`。
- `update_training_settings()` 保存我方设置并使旧应手缓存失效；`build_training_task()` / `refresh_training_task()` / `append_training_session()` 负责训练题与历史；`load_training_cache()` / `save_training_cache()` 负责独立应手树，删除棋谱时同步清理。
- 坏文件容错（跳过并记失败列表）。
- 可被 `test_game_library.py` **无头**测试（自动入库/重复导入/快照更新/收件箱扫描/去重保留缓存/坏文件）。

#### 9.11.9 [training.py](analyzer/training.py) — 阶段训练纯逻辑（v3.27）
- `generate_training_task(tree, window=36, step=12, player_color=None)`：从已分析主线生成「最差阶段训练题」。窗口约 36 手、滑动步长 12 手；可按 B/W/both 过滤统计口径；评分综合累计目损、胜率损失、疑问/恶手数量、问题手数量和连续问题手。
- `describe_training_task(task)`：生成棋局库/状态栏可读摘要，如「中盘第 25-48 手：平均目损 5.0…」。
- `grade_training_session(task, evaluations, user_color)`：训练结束评价，只统计用户执棋手；与原阶段同方平均目损比较，输出 A/B/C/D、改善值、参考棋力档、等级统计、训练好手、重点问题手和下一轮建议。
- 与 tkinter/KataGo 解耦，可被 `test_training.py` **无头**测试。

#### 9.11.10 [review_report.py](analyzer/review_report.py) — Markdown 复盘报告（v3.24）
- `generate_report(tree, komi, rules, score_result) -> str`：生成 Markdown 复盘报告。
- 章节：结论摘要 + 稳健单局表现 + 自动文字复盘 + 三阶段表现 + 有效问题棋；不导出逐手评价表。
- 复用 `review.ReviewReport` 数据，纯逻辑可无头测试（`test_review_report.py`）。

#### 9.11.11 [move_quality.py](analyzer/move_quality.py) — 精细手段评价引擎（v4.0）
- **6 级评价**（`QUALITY_*`）：最佳 / 好手 / 一般 / 不佳 / 恶手 / 未评价，基于目损 + 胜率损失 + AI 排名综合评分（0–100）。
- `MoveQualityInput`/`MoveQualityResult`：输入走子方视角目损/胜率/AI排名/visits/stage，输出评价标签 + 评分 + 原因文案 + 置信度（high/medium/low/unknown）+ 8 种问题标签。
- **胜负已定降级**：`is_meaningful_position` 判定，胜负已定时评价上限封到「不佳」。
- 不依赖 tkinter / KataGo 进程，可被 `test_move_quality.py` **无头**测试。

#### 9.11.12 [player_profile.py](analyzer/player_profile.py) — 长期个人画像（v4.0）
- **核心原则**：按有效手数加权聚合（不是「各盘平均的平均」），只取 `confidence≠unknown` 且 `is_meaningful_position=True` 的手为主要样本。
- **数据类**：`GameProfileSummary`（单局轻量摘要）→ `ProfileStats`（聚合统计）→ `PlayerProfile`（完整画像：总体/黑白/三阶段统计 + 趋势 + 质量分布 + 问题标签分布 + 优弱点 + 推荐）。
- **入口**：`build_game_profile_summary(quality_results)` → `build_profile(games_data, user_side, window_games)` → `build_profile_insights(profile)`（生成中文优弱点/推荐，带样本数据佐证，不输出段位）。
- **趋势**（`profile_trend`）：最近 5 盘 vs 前 10 盘窗口比较，方向 improving/stable/declining/insufficient。
- 不依赖 tkinter，可被 `test_player_profile.py` **无头**测试（加权聚合/身份/趋势/建议）。

#### 9.11.13 [training_analysis.py](analyzer/training_analysis.py) — 训练后分析（v4.0）
- `analyze_training(original_moves, training_moves, phase)` → `TrainingAnalysis`：训练后把用户训练手与原实战手按精确位置/同步两层级面对齐，分类 repeated_error / new_error / improved / neutral。
- **训练评分**：`100 − avg_loss×8 − blunder×15 − …`，<4 手样本不足，hint 比率 >50% 封顶 85，重复恶手封顶 74。
- **复习计划**：重复恶手→1d，分数<60→3d，60-74→5d，75-89→7d，90+无重复→14d。
- 不依赖 tkinter，可被 `test_training_analysis.py` **无头**测试。

#### 9.11.14 [profile_store.py](analyzer/profile_store.py) — 画像缓存持久化（v4.0）
- `save_profile`/`load_profile`：JSON 原子写（tmp+os.replace），来源指纹（SHA1）判定缓存新鲜度。
- `rebuild_profile_from_library(index_path, cache_path)`：从 `game_library/index.json` 重建画像。
- 旧/损坏数据降级返回 None，不抛异常。可被 `test_profile_store.py` **无头**测试。

#### 9.11.15 [style_profile.py](analyzer/style_profile.py) — 8 维个人棋风画像（v4.0）
- 8 个维度（接触战/厚薄/地盘/攻击/防守/急所/收官/中央）从 `MoveQualityResult` 的问题标签统计每百手频率 + 平均代价。
- `build_style_profile(games_data, user_side)` → `StyleProfile`，含每维度频率/代价/趋势/置信度/代表局面。
- 不依赖 tkinter，可被 `test_style_profile.py` **无头**测试。

#### 9.11.16 [growth_path.py](analyzer/growth_path.py) — 成长路线（v4.0）
- `build_growth_path(style_profile, player_profile)` → `GrowthPath`：生成**一个**主线目标 + 保留风格 + 修正习惯 + 观察项，每项带代表局面。
- 数量约束：1 主线 + ≤2 保留 + ≤2 修正 + ≤2 观察。可被 `test_growth_path.py` **无头**测试。

#### 9.11.17 [deep_verification.py](analyzer/deep_verification.py) — 高 visits 复核队列（v4.0）
- 把画像/棋风/成长路线的关键结论（如「最弱阶段」）排入高 visits 复核队列。
- 原子持久化 + 稳定性汇总（多次高 visits 结果一致性）+ 结论降级（不稳定结论标记为低置信）。
- 可被 `test_deep_verification.py` **无头**测试。

#### 9.11.18 [mistake_book.py](analyzer/mistake_book.py) — 跨棋局错题本（v4.0）
- 从画像问题手自动同步高价值错题到 `game_library/mistake_book.json`。
- 支持身份过滤（只看执黑/执白）、隐藏答案测验、间隔复习调度（1d/3d/7d/14d 倍增）。
- 重分析/删除棋谱时联动清理。可被 `test_mistake_book.py` **无头**测试。

#### 9.11.19 [profile_report.py](analyzer/profile_report.py) — 个人分析 Markdown 报告（v4.0）
- `generate_profile_report(profile, style_profile, growth_path)` → Markdown 报告。
- 章节：核心指标 + 单局基线 + 黑白/阶段表现 + 逐盘趋势 + 棋风维度 + 成长路线 + 到期错题。
- 可被 `test_profile_report.py` **无头**测试。

#### 9.11.20 [style_view.py](analyzer/style_view.py) — 独立棋风窗口（v4.0）
- `StyleViewWindow`（Toplevel）：维度表（8 维频率/代价/趋势/置信度）+ 成长路线 + 报告导出 + 复核队列。

### 9.12 已知问题与限制

- **数据持久化已实现**（v3.21–v3.24）：项目文件 `.kga.json` 保存整棵树 + analysis 缓存 + 点目结果 + RE + 规则/贴目；棋谱库自动入库（inbox/sgf/projects/index.json）+ 搜索/删除/历史；Markdown 报告导出。**局限**：`.kga.json` 是私有格式（不兼容其他软件）；棋谱库单机本地（无云同步）；报告为静态 Markdown（不含棋盘图）。
- **易用性已实现**（v3.11）：自动启动引擎（失败安静跳过、加载中可落子）、快捷键（Home/End/Space/PgUp/PgDn）、自动播放（▶ 按钮/Space，到末尾自停、手动操作自停）。自动播放间隔 `PLAY_MS=800ms` 为常量，暂未做设置项。
- **复盘三件套已实现**（v3.10）：批量分析整盘（导航零等待）、每手目损标注 + 失误榜 Top 8（双击跳转）、胜率曲线（点击跳转）。目损阈值/Top N 为常量（`LOSS_THRESHOLD=2.0`/`REVIEW_TOP_N=8`），暂未做设置项；曲线只画 scoreLead（黑视角），未画 winrate 第二条。
- **终局点目已实现**（v3.9，中国规则面积数法）：标记死子即算黑/白面积与胜负、双 Pass 提示进入点目、ownership 死子建议、确认后 SGF 写 `RE[]`+根 `C[]` 摘要（见 [§9.10](#910-终局点目中国规则--面积数法)）。**暂不支持**：日本规则精确点目、自动死活判定替代人工、多规则计分切换、复杂双活、提子数计税、非 19 路——死活最终以用户标记为准。
- **Pass 已真实实现**（v3.2）：`do_pass` 创建真实 pass 节点、`moves_list` 输出 `["B","pass"]`、KataGo 查询带 pass、SGF 进出 `B[]/W[]`（真实引擎验证）。
- **SGF 完整支持**（v3.3/v3.7/v3.9）：变化图 variations、C[] 注释、AI 分析摘要写入 C[]、基本转义、让子棋 HA/AB/AW、终局 `RE[]`/点目摘要均支持。暂不支持：复杂标记(TR/MA/CR)、多 game tree、非 19 路。
- **配置持久化**（v3.1）：`app.py` **无 `D:\katago` 硬编码**；`config_manager.py` 自动定位 katago-runtime、设置存 `user_settings.json`、启动前检查 exe/model/cfg/DLL。
- **异步防污染**（v3.5）：`analysis_guard.py` 按 engine_session_id 丢弃跨引擎实例的过期结果；仅当前节点结果刷新 UI。
- **热力图映射已验证**（v3.6，v3.8 加固）：经 `heatmap.py` + `test_heatmap_mapping.py` 确认——索引映射 `y*size+x`、policy 去末位 pass、ownership 符号(+黑/−白)，并用**单子局面**（黑下 Q16 后该点强黑属地、比对角更黑）真正检验 y/x 方向无翻转（空盘对称测不出翻转，已弃用）。仍为 stipple 半透明（真 alpha 需 Pillow）。
- **本地规则简化**：`board.py` 只做提子/自杀/简单打劫；复杂劫/终局以 KataGo 返回为准。
- **GBK 控制台 ✅ 编码**：`test_ipc.py` 已做 UTF-8 重配（不再崩）；其余脚本打印 emoji 时仍可能乱码，用 `python -X utf8` 规避。

### 9.13 数据持久化与报告导出

**复盘项目文件**（`.kga.json`，v3.21，[project_store.py](analyzer/project_store.py)）：SGF 适合交换棋谱，项目文件适合**继续复盘**。点【保存项目】生成 `.kga.json`，保存整棵棋局树（含变化图）、每个节点的 KataGo `analysis` 缓存、当前停留节点、评论、点目结果、导入 SGF 的原 `RE[]`、规则与贴目。点【打开项目】恢复——已分析节点不再等待 KataGo。继续落子/分析后可再次保存。

**本地棋谱库**（v3.22，[game_library.py](analyzer/game_library.py)）：导入 SGF 后自动把原谱和项目快照存入 `analyzer/game_library/`：

| 子目录/文件 | 作用 |
|---|---|
| `inbox/` | 自动采集收件箱——放入 `.sgf`，启动或打开棋谱库时自动扫描、去重并入库 |
| `sgf/` | 原始 SGF |
| `projects/` | `.kga.json` 项目快照 |
| `index.json` | 棋谱索引 |

【棋谱库】窗口：搜索（名/规则/路径）、列表（手数/已分析节点数/我方/训练题/最近训练/规则/贴目/最近时间）、双击从快照继续复盘、删除（移除库内文件不删原始来源）、分析回流自动更新库内项目。主线分析完成后会在索引里生成 `trainingTask`；点【我执黑】/【我执白】/【双方】改变 `playerColor`；点【开始阶段训练】可从最差阶段前一手开始与 AI 重下，训练评价写入 `trainingSessions`，点【训练历史】可查看。

**阶段训练模式**（v3.27，[training.py](analyzer/training.py)）：棋局库自动维护每盘棋的最差 30-40 手训练题。训练题由用户执棋方的累计目损、胜率损失、疑问/恶手和连续问题手综合选出；训练时用户重下该阶段，AI 自动应手，候选点默认隐藏；结束后只统计用户执棋手，输出训练评分、改善幅度、训练好手、重点问题手和下一轮建议。

**Markdown 复盘报告导出**（v3.24/v3.35，[review_report.py](analyzer/review_report.py)）：点【导出报告】生成 `.md`，包含稳健单局表现、自动文字复盘、三阶段表现、有效问题棋和恶手意图对比；移除冗长逐手表。

### 9.14 路线（v3 稳定复盘版 + 终局点目 + 复盘三件套 + 易用性 + 持久化，已完成）

**v2 基础**（均 ✅）：BoardState+MoveTree · 棋盘 UI · analysis 通信 · AI 推荐点 · 撤回/快进/分支 + 分支←/→ + 棋局树视图 · SGF 导入/导出 · 地盘+策略热力图 · 模型管理

**v3 可靠性 + 补全**（均 ✅，各有专属测试）：
- **v3.1** 配置管理：[config_manager.py](analyzer/config_manager.py) 自动定位 + `user_settings.json` 持久化 + 启动前检查（[test_config_manager.py](analyzer/test_config_manager.py)）
- **v3.2** Pass 真实提交：`board.pass_move`/`movetree.play_pass`/SGF `B[]/W[]`（[test_pass.py](analyzer/test_pass.py)，真实引擎验证）
- **v3.3** SGF 变化图 + C[] 注释 + AI 摘要 + 转义（[test_sgf_variations.py](analyzer/test_sgf_variations.py)）
- **v3.4** AI 推荐点双击落子（ttk.Treeview，复用/新建分支）
- **v3.5** 异步请求守卫：[analysis_guard.py](analyzer/analysis_guard.py) session 隔离过期结果（[test_analysis_request_guard.py](analyzer/test_analysis_request_guard.py)）
- **v3.6** 热力图映射验证：[heatmap.py](analyzer/heatmap.py) + [test_heatmap_mapping.py](analyzer/test_heatmap_mapping.py)（真实引擎 180° 对称）
- **v3.7** 让子棋 setup：HA/AB/AW + `initialStones` 查询（test_sgf_variations 内验证）
- **v3.8** 对抗式审查与加固：17-agent 并行审查全部 v3 代码（每条发现再独立对抗式核实）→ **无 critical/major 实质 bug**（多数原始发现经核实为误报）。应用 5 处加固：① `set_initial_stones` 加可选 `to_move`（让子首手方可显式指定）② `AnalysisGuard.invalidate_node` + `_request_analysis` 请求前清该节点旧挂账（防延迟响应覆盖）③ `_render_analysis` 在 winrate 缺失时重置胜率条（防上一节点残留）④ `do_import_sgf` 导入后补刷树视图窗口 ⑤ 热力图测试改用单子局面真正检验方向。修复后全部测试仍绿。
- **v3.9** 终局点目（中国规则 / 面积数法）：[score_estimator.py](analyzer/score_estimator.py) 死子移除 + flood fill 空区归属 + ownership 死子建议；app.py 点目模式（地盘图 / 死子红 X / 实时结果面板 / 双 Pass 提示 / 模式隔离）；sgf.py 确认后写 `RE[]` + 根 `C[]` 点目摘要（保留原 RE）；movetree.py 加 `score_result`/`double_pass`。经 26-agent 对抗式审查（6 维度 + 逐条复核）发现并修复 **7 项缺陷**（分支/树视图在点目模式未拦截致失同步、ownership 短数组/死子点串越界、状态栏被 80ms 轮询覆盖、退出后分析面板陈旧等），逐项补回归测试。
- **v3.10** 复盘三件套：[review.py](analyzer/review.py) 每手「目损」纯逻辑（黑/白走子方视角翻转）+ 失误榜 + 胜率/目差序列；app.py【分析整盘】批量分析（主线并发，`analysis_guard` 防污染，导航零等待）+ 进度、失误 Top 8 榜（双击跳转）、棋盘红圈标注、【胜率曲线】窗口（Canvas 折线 + 失误红点 + 点击跳转 + 实时刷新）。
- **v3.11** 易用性：`_maybe_autostart()` 打开即后台拉起引擎（quiet，失败安静跳过，加载中可落子、就绪后自动补分析）；快捷键 `Home/End`(首尾)/`Space`(自动播放)/`PgUp/PgDn`(翻 10 手)；`_start_auto_play`/`_auto_play_step`/`_stop_auto_play` 每 `PLAY_MS` 沿主线播放，到末尾自停、任何手动操作自停。同时把 3 个 UI 冒烟合并为单 app `test_ui_smoke.py`，消除 Windows Tcl 多 root 偶发 `invalid command name` 不稳。
- **v3.12** 棋盘可读性 + 形势判断：① 候选点字母由 10 号加白底圆升级为 **13 号粗体 + 白底**（高对比，远距离可读）；② 修 `_draw_ownership` 的 stipple 密度**画反**bug（原强地盘反而稀疏），改按 |ownership| 三档（gray12 铁地盘 / gray25 倾向 / gray50 争议），黑/白地盘归属一目了然；③ 胜率条下加大字「**形势：黑/白 +X.X 目**」标签（`_render_score_judgment`，黑视角 scoreLead）。
- **v3.16** 棋盘质感精修：① 棋盘外加**落影边框**（外层柔和阴影色 Frame，右下偏移模拟光源左上 + canvas 内棕色细边框）；② 棋子 **3 层高光球感**（黑子双层亮高光、白子右下球阴影 + 左上纯白高光）；③ 星位改**双圈**（外环描边 + 内实心点）；④ 坐标改等宽 Consolas 9 号；⑤ 主标题升 h1（15 号粗）。
- **v3.17** 主变 10 步：【主变 10 步】按钮一键显示首选 `moveInfos[0].pv[:10]`——棋盘带圈数字 1–10（黑白底按当前轮次交替、主色蓝描边）+ 状态栏序列；与候选点互斥；不足 10 取实际、pass 跳过、点目模式不显示。
- **v3.18** 前三选主变切换：主变模式加 `_pv_idx`，主变模式下点候选行（`_on_move_select`）即切换显示该选（第 1/2/3 选…）的主变；`_pv_pick` 索引**实际显示的候选列表**（`_pv_candidates`，与 tv_moves 行索引一致，避免 pass/无效候选被跳过后行索引与 moveInfos 索引 off-by）+ clamp 写回 `_pv_idx` 归一化；序列标注「第 N 选」；toggle 开启默认第 1 选。
- **v3.19** 整盘复盘报告 + AI 评价分级：`review.py` 加 `grade_of(loss)`（好<1/普通 1–3/疑问 3–6/恶≥6）+ `grade_summary()` 统计；app.py「失误榜 Top 8」改为**逐手评价表**（全盘每手，列含等级，双击跳转）+ 全盘概览 label（好 X·疑问 Y·恶 Z·共 N）；redraw 当前手按等级色圈（好绿/疑问橙/恶红，普通不标）替代原 `loss≥2` 红圈。
- **v3.20** 对局评分（参考棋力档）：`review.py` 加 `MoveEvaluation.agreement_rank`（actual 在父 moveInfos 前 5 选排名）+ `RANK_BANDS`/`rank_of`（平均目损→业余段级位 7 档）+ `player_stats(color)`/`player_rank(color)`；app.py 形势卡片加 `lbl_rating` 面板（黑/白参考棋力档 + 平均目损 + 首选吻合度，标注「AI 视角参考」）。
- **v3.21** 复盘项目文件（分析缓存持久化）：新增 [project_store.py](analyzer/project_store.py)，以 `.kga.json` 保存/打开整棵 MoveTree、每个节点的 KataGo `analysis` 缓存、评论、终局点目结果、导入 SGF 的原 `RE[]`、当前节点路径、规则与贴目；app.py 棋谱卡片加【打开项目】/【保存项目】，打开后恢复缓存并刷新候选点、逐手评价、评分和曲线；新增 [test_project_store.py](analyzer/test_project_store.py) 覆盖变化图、当前节点、analysis 缓存、点目结果和磁盘往返。
- **v3.22** 本地棋谱库 + 速度预设：新增 [game_library.py](analyzer/game_library.py)，导入 SGF 后自动保存原谱到 `game_library/sgf/`、项目快照到 `game_library/projects/`、索引到 `game_library/index.json`；app.py 棋谱卡片加【棋谱库】窗口，支持搜索、双击打开、删除记录（不删原始来源文件）、最近打开时间、已分析节点数；分析回流后自动更新当前库内项目；设置窗口加 `快 80 / 标准 200 / 深入 800` 三档 `maxVisits` 预设，便于在速度和深度之间快速切换。
- **v3.23** 自动棋局库收件箱：`game_library/inbox/` 作为自动采集目录；程序启动/打开棋局库时自动扫描 `.sgf`，按 SGF 内容 SHA1 去重，生成原谱与 `.kga.json` 项目快照；重复扫描不覆盖已有分析缓存；坏文件进入 failed 列表；棋局库窗口新增【打开收件箱】/【扫描收件箱】。
- **v3.24** 复盘报告增强：`review_report.py` 从基础表格升级为可读复盘报告，包含结论摘要、对局评分、全盘概览、下得好的地方、需要重点复盘的问题手、关键失误 Top 10、逐手评价；`test_review_report.py` 覆盖摘要/好棋/问题手章节。
- **v3.25** UI 清晰化重构：右侧由 5 张纵向卡片改为固定「总览」+ `ttk.Notebook` 标签页（AI / 复盘 / 棋谱 / 导航），胜率/目差/形势/评分常驻，总操作区按场景分开；调色板更新为更克制的中性浅灰 + 清晰蓝，减少说明文字和视觉拥挤。
- **v3.26** 三阶段水平分析：`review.py` 新增 `phase_summary()` / `phase_stats()` / `phase_of_move()`，自动将主线划分为布局/中盘/关子，并计算每阶段平均目损、阶段水平、好/普通/疑问/恶、代表好手和重点问题手；UI 复盘摘要显示三阶段水平；Markdown 报告新增「三阶段水平分析」表。
- **v3.27** 阶段训练模式：新增 [training.py](analyzer/training.py)，按约 36 手滑动窗口自动寻找累计表现最差阶段；支持「我执黑/我执白/双方」训练口径，同口径比较原阶段与训练阶段；`game_library.py` 在项目快照更新时写入 `trainingTask/trainingSummary`，训练结束追加 `trainingSessions`；app.py 棋局库新增「我方/训练题/最近训练」列、【我执黑/我执白/双方】、【开始阶段训练】、【训练历史】，训练页新增【提示/结束训练/重来/原实战】，训练中隐藏候选点，用户从问题阶段前一手重下，AI 自动按首选应手，结束后自动评价用户训练内容；新增 [test_training.py](analyzer/test_training.py)。
- **v3.28** 训练入口自动准备 + 领地标识增强：棋谱库【开始阶段训练】在无训练题时不再要求手动分析，而是通过 `_pending_training_record_id` 自动启动/等待 KataGo、分析整盘、生成训练题并直接进入训练；导入入库自动分析也会尝试静默启动引擎。地盘热力图从整格 stipple 改为清晰交叉点领地标记；点目模式黑/白/中立地标记加大，读数更接近主流围棋平台。
- **v3.29** GPU 默认 + 训练速度模式 + 棋局库后台准备：`config_manager.py` 默认/下拉优先 `katago-opencl.exe`，`user_settings.json` 切到 OpenCL；新增 `training_speed_mode`（快速/均衡/精细）和 `library_training_visits`；app.py 将训练对弈请求改为低 visits 快速分析，并新增棋局库后台 offscreen 分析队列，为离线棋局预生成训练题。
- **v3.30** 阶段训练静默预热：新增训练预热缓存 `_training_prefetch_*`；用户回合到来后后台用当前局面 `moveInfos` 预分析前几候选，用户落子命中则直接复用 analysis，降低 AI 应手等待；训练中不主动纠错、不提示思路，保持连续对弈，结束后统一评价。
- **v3.31** 稳健棋力估计与文字复盘：排除胜负已定局面、单手目损封顶、最小样本和估计区间；以野狐官方 5 段对局校准；阶段改用质量评价；复盘页与 Markdown 报告新增自动中文棋局解说；SGF 黑白姓名持久化。
- **v3.32** 复盘信息架构重构：复盘页按工具/训练/阶段/对局分析/问题棋分框；长文字带滚动条；移除 UI 与报告逐手评价；总览棋力改为固定列宽双行表；AI 候选由 6 个缩减为 3 个。
- **v3.33** AI 三选推荐模块：移除棋盘候选圆圈和候选 Treeview；右侧改为 A/B/C 三个固定按钮，逐项显示当前行棋方胜率；单击看 PV、双击落子，并继续支持主变切换。
- **v3.34** 合法落点悬停预览：鼠标靠近空交叉点时显示当前方虚化棋子；复用棋盘规则过滤占位、自杀和打劫；使用网点透明感与虚线轮廓区别真实棋子，并处理离开/落子/导航/点目/主变/训练回合清理。
- **v3.35** 恶手选点意图对比：只对目损 ≥ 6 的恶手，分别模拟实战点和 AI 首选，综合角边中区域、敌我邻接、连接/接触、提子、落子后气数与 AI 主变生成“可能意图”和核心差异；问题棋模块新增可滚动详情，Markdown 报告同步输出。该内容是可追溯的棋形启发式推断，不宣称读取用户真实心理。
- **v3.36** 训练应手持久化：新增 [training_cache.py](analyzer/training_cache.py) 和独立 `game_library/training_cache/`；训练题生成后自动预演最多 20 个回合，每回合覆盖用户前三选、AI 应手和应手后局面；棋局库新增「快速训练准备」状态。训练对弈改为磁盘缓存优先，命中时无需启动 KataGo；未覆盖分支静默启动引擎补算并回写。缓存按模型、规则、贴目、训练 visits 和我方设置自动失效；新增缓存及棋局库生命周期测试。
- **v3.37** 全模式提示与悔棋：棋盘下方新增不受标签页影响的常驻操作和 `F1`/`Ctrl+Z` 快捷键；提示支持缓存命中、异步分析完成后自动显示、棋盘落点标记和点目建议。训练悔棋按完整用户回合回滚，撤销用户上一手、AI 应手、训练计数及挂起请求；新增 [test_training_controls.py](analyzer/test_training_controls.py) 无界面回归。
- **v3.38** 问题手双分支深度对比：新增 [branch_comparison.py](analyzer/branch_comparison.py)；自动队列深算最严重 3 手，实战与 AI 分支使用同 visits，并量化胜率、目差、ownership 控制点和 14 手主变。复盘问题棋模块新增三种视图与棋盘编号变化，结果随 `.kga.json` 持久化并进入 Markdown 报告；新增纯逻辑、异步汇总、项目往返和报告测试。
- **v3.23** 配置窗口改进：引擎/模型改**下拉框**（自动列出 `katago-runtime` 中 CPU/GPU 引擎 + `models/*.bin.gz`）；加【检测配置】启动前检查（exe/model/cfg/DLL）；【应用】写入 `user_settings.json`，引擎/模型变更时自动重启。
- **v3.24** Markdown 复盘报告导出：新增 [review_report.py](analyzer/review_report.py)（`generate_report`）——生成结论摘要 + 黑白参考棋力档 + 全盘等级概览 + 下得好的地方 + 重点问题手 + 关键失误 Top + 逐手评价表 + 终局结果；app.py 棋谱卡片加【导出报告】；新增 [test_review_report.py](analyzer/test_review_report.py)。
- **v3.13** 清爽浅色现代风（界面换皮）：新增 `COLORS`/`FONTS` 调色板 + `_setup_style()`（ttk `clam` 主题，自定义 `TButton`/`Accent.TButton`/`Treeview`/`TLabelframe`）；全部 `tk.Button→ttk.Button`（启动/分析整盘/确认结果用蓝色 `Accent.TButton`）；面板背景 `#f5f6f8`、棋盘 `#e8b96b`、主色蓝 `#1a5fd0`、墨黑文字、微软雅黑 UI 字体；棋子加左上高光（`_draw_stone` 瓷质感）；网格/星位/坐标/胜率条/候选圈/地盘图/点目叠加/胜率曲线/三个 Toplevel（曲线·树视图·引擎设置）全部对齐调色板。零外部依赖（纯 ttk + Canvas + 配色）。
- **v3.14** 形势判断领地统计 + 棋局进度条：① `_render_territory` 基于 ownership 统计黑/白控制点数（|v|>0.05 计入），形势标签下加 `领地：黑 X · 白 Y`（与精算 scoreLead 互补）；② 棋局进度条 `ttk.Scale`（播放按钮下）—— 拖动/点击跳主线第 N 手（`_on_scale`→`ReviewReport.node_at_move`），`lbl_scale` 显示 `N/M`，`_update_scale` 在导航/落子/播放/导入后同步范围与位置，`_scale_suppress` 防 set↔command 反馈循环，点目模式拦截跳转。
- **v3.15** 卡片化布局重构（治「粗糙」）：`_build_ui` 右侧由一长串平铺改为 **5 张 `Card.TLabelframe` 卡片**（对弈 / AI 分析 / 形势 / 复盘 / 棋谱）；卡片内 `grid` 对齐、按钮等宽（`sticky="ew"`），主操作用 `Accent.TButton` 蓝填充；删去 `_sep` 简陋分隔线，改卡片间 `pady` 留白；新增 `Card.TLabelframe` Style（1px 细边框 + 主色标题）。拆出 `_build_card_play/analysis/score/review/sgf` 五个构建方法。零功能改动、所有 widget 名保留、57 测试全绿。
- **v3.26** 对局评分分阶段（布局/中盘/官子）：`review.py` 加 `PHASES`/`PHASE_LABELS`/`PHASE_ALIASES`/`normalize_phase`/`_phase_range`（主线总手数等分 33%/66%）/`phase_summary()`（每段 avg_loss/rank/problem_count）/`rank_short`（档位简称）；`player_stats(color, phase)` 支持中英文 phase 别名；app.py `_render_rating` 显示全盘总评 + 三阶段细分（`布局 高段(0.8) ｜ 中盘 初段(1.5) ｜ 官子 强段(0.3)`）。
- **v3.27** 全屏自适应缩放：`resizable(True, True)` 解锁窗口缩放 + `<Configure>` 防抖（80ms）→ `_do_resize` 按窗口高度重算 `CELL/MARGIN/BOARD_PIX`（12–50px）+ Canvas 同步 + redraw（棋盘/棋子/坐标/候选/pv/三角/评价圈全部随窗口缩放）；**F11 全屏 / ESC 退出**。
- **v4.0** 个人长期训练分析器：新增 13 个纯逻辑模块 + UI 集成，把项目从「单盘复盘工具」升级为「个人长期训练分析器」。
  - **精细手段评价引擎** [move_quality.py](analyzer/move_quality.py)：6 级评价 + 综合评分 + 置信度 + 胜负降级 + 原因 + 问题标签。
  - **长期个人画像** [player_profile.py](analyzer/player_profile.py)：按有效手数加权聚合，趋势/阶段/黑白/质量分布/问题标签/优弱点。
  - **训练后分析** [training_analysis.py](analyzer/training_analysis.py)：局面对齐/重复错误/新错误/改善 + 训练评分 + 复习计划。
  - **画像持久化** [profile_store.py](analyzer/profile_store.py)：JSON 原子写 + 指纹 + 增量更新 + 从库重建。
  - **个人报告** [profile_report.py](analyzer/profile_report.py)：Markdown 报告（核心指标/趋势/棋风/成长/错题）。
  - **8 维棋风** [style_profile.py](analyzer/style_profile.py) + [style_cost.py](analyzer/style_cost.py) + [growth_path.py](analyzer/growth_path.py) + [style_report.py](analyzer/style_report.py) + [style_store.py](analyzer/style_store.py) + [style_view.py](analyzer/style_view.py)。
  - **高 visits 复核** [deep_verification.py](analyzer/deep_verification.py)：关键结论复核队列 + 稳定性汇总 + 降级。
  - **跨棋局错题本** [mistake_book.py](analyzer/mistake_book.py)：画像同步 + 隐藏答案测验 + 间隔复习调度。
  - 全部纯逻辑、无 tkinter 依赖，各有专属测试（共 ~50 新测试）。

**全部测试通过（137 项）**：`test_score_estimator.py` / `test_review.py` / `test_move_quality.py` / `test_player_profile.py` / `test_training_analysis.py` / `test_profile_store.py` / `test_profile_report.py` / `test_style_profile.py` / `test_style_cost.py` / `test_growth_path.py` / `test_style_report.py` / `test_deep_verification.py` / `test_mistake_book.py` / `test_branch_comparison.py` / `test_review_report.py` / `test_project_store.py` / `test_game_library.py` / `test_training.py` / `test_training_cache.py` / `test_training_controls.py` / `test_ui_smoke.py` / `test_ipc.py` / `test_pass.py` / `test_sgf_variations.py` / `test_config_manager.py` / `test_analysis_request_guard.py` / `test_heatmap_mapping.py` + `smoke_app.py`（`python -m py_compile analyzer/*.py` 亦通过）。

> **仍可扩展**：曲线加 winrate 第二条 + 阈值/Top N/播放速度设置项、节点文字笔记、日本规则精确点目、复杂双活(seki)判定、`TB`/`TW` 地盘标记导出、真半透明热力图(Pillow)、复杂 SGF 标记(TR/MA/CR)、多 game tree、时间制。

---

## 10. 从源码编译

详见 [KataGo/Compiling.md](KataGo/Compiling.md)。要点：

- **语言**：C++，至少 C++14。
- **构建系统**：CMake ≥ 3.18.2（Windows 强烈建议用 CMake GUI）。
- **平台 / 工具链**：
  - Linux：g++ + make；
  - macOS：AppleClang + Swift（Metal 后端）+ Ninja；
  - Windows：MSVC 2017+ 或 MinGW（MinGW 仅支持 Eigen/OpenCL，不支持 CUDA/TRT）。
- **依赖**：`zlib` + `libzip`（自训练需要，可用 `-DNO_LIBZIP=1` 关闭）；按后端另需 Eigen3 / CUDA+cuDNN / TensorRT≥8.5 / Metal 工具链。可选：TCMalloc（自训练强烈建议）、OpenSSL（分布式必需）。
- **选后端**：`-DUSE_BACKEND=<OPENCL|CUDA|TENSORRT|EIGEN|METAL>`。常用旗：`-DUSE_AVX2=1`（Eigen，显著加速，老 CPU 不支持）、`-DUSE_TCMALLOC=1`、`-DNO_GIT_REVISION=1`。
- **一行示例（Linux OpenCL）**：
  ```bash
  git clone https://github.com/lightvector/KataGo.git
  cd KataGo/cpp && cmake . -DUSE_BACKEND=OPENCL && make -j4
  ```
- **分布式贡献编译**（`contribute`）：需 `-DBUILD_DISTRIBUTED=1` + OpenSSL，且**必须在 git clone 内构建**（以嵌入 git hash）；**不允许用 master 分支**——用最新 release tag 或 `stable` 分支。

> 本项目已提供预编译好的 [katago-runtime/](katago-runtime/)（CPU + OpenCL），除非要改 C++ 引擎本身，否则无需自行编译。

---

## 11. 自训练（Self-play）管线

详见 [KataGo/SelfplayTraining.md](KataGo/SelfplayTraining.md)。完整训练闭环需 **5 个并发部件**：

| # | 部件 | 语言 | 作用 |
|---|---|---|---|
| 1 | **selfplay 引擎** | C++ (`katago selfplay`) | 用当前最优网持续自对弈、写训练数据；模型目录为空时用 RNG——是闭环的**起点** |
| 2 | **shuffler** | Python (`python/shuffle.py`) | 扫描自对弈数据、洗牌成 npz，管理窗口/回看策略 |
| 3 | **trainer** | Python (`python/train.py`) | 在 npz 上持续训练，定期快照；默认 `b6c96`，可改 `python/katago/train/modelconfigs.py` |
| 4 | **exporter** | Python (`python/export_model.py`) | PyTorch `.ckpt` → C++ 模型格式 |
| 5 | **gatekeeper**（可选） | C++ (`katago gatekeeper`) | 让新网与当前最优网对弈，胜者晋升为 selfplay 使用的网 |

两种运行模式：
- **同步单机**：`python/selfplay/synchronous_loop.sh`，顺序跑 5 步（默认参数刻意偏小，仅用于起步）；
- **异步多机**（官方 run 用的方式）：5 个进程各自常驻，共享一个网络文件系统上的 `$BASEDIR`。建议 **selfplay 的 GPU 投入是 training 的 4×–40×**。

---

## 12. 常见问题与排错（FAQ）

### 12.1 ⚠️ 启动即崩溃：`STATUS_DLL_NOT_FOUND (0xC0000135)`（**本项目部署时亲历**）

**症状**：双击或运行 `katago-*.exe`，瞬间退出，无任何输出/日志，Windows 弹「应用程序无法正常启动」。退出码 `0xC0000135`（`-1073741515`）。

**根因**：引擎依赖 4 个随发行包附带的第三方 DLL，**必须与 `katago.exe` 同目录**：
- `libcrypto-3-x64.dll`（OpenSSL 加密）
- `libssl-3-x64.dll`（OpenSSL TLS）
- `libz.dll`（zlib，解压 `.bin.gz` 模型）
- `libzip.dll`（libzip，SGF/归档处理）
- 另 `cacert.pem`（HTTPS 校验，如分布式训练）

Windows DLL 加载器按「exe 目录 → System32 → PATH」查找。若只复制了 `katago.exe` 而漏掉这些 DLL，进程**在加载模型之前就死了**，没有任何 KataGo 报错。

**修复**：把上述 4 个 DLL + `cacert.pem` 放到与 `katago-*.exe` **同一目录**。本环境 [katago-runtime/](katago-runtime/) 已正确布置。

> 注：`msvcp140*.dll` / `vcruntime140*.dll` 是捆绑的 MSVC 运行时；若系统已装 VC++ Redistributable 且发生冲突，可删除捆绑副本，但通常保留即可。

### 12.2 找不到模型/配置

报错 `-config ... was not specified ... and default was not found`：确认 `default_gtp.cfg` 与 `default_model.bin.gz` 与 `katago.exe` 同目录（Windows 仅搜该目录）。

### 12.3 OpenCL 找不到平台 / 显卡驱动问题

- 部分显卡 OpenCL 驱动有缺陷（如 AMD RX 5700、Mesa OpenCL、某些 Intel 核显）；
- 提示 `Found OpenCL Platform ... (Mesa)` → 换驱动；
- 无可用 GPU → 退回 CPU 版 `katago-eigenavx2.exe`。

### 12.4 在 GUI（Lizzie/Sabaki/q5go）里「卡住、永远加载中」

通常是配置/路径/GPU 问题，而 GUI 往往吞掉 KataGo 的报错。**直接在命令行跑 `benchmark` 或 `gtp` 看真实错误**。首次在新网络尺寸上运行也会做耗时数分钟的调优。

### 12.5 路径含空格 / 调优时无法写文件

- 避免文件/目录名含空格（不同 GUI 引号转义不一）；
- 调优报 `Could not create file`：KataGo 对所在目录无写权限（如 `Program Files`），移到别处。

### 12.6 analyzer 报「找不到引擎 / 找不到模型」

分析器启动时会从当前位置向上查找 `katago-runtime`，并把引擎/模型路径持久化到 `analyzer/user_settings.json`。若自动定位失败，点【引擎/模型…】手动选择 `katago-*.exe` 和 `*.bin.gz`，再点【检测配置】查看具体缺失项；路径保存后下次启动会继续使用。

### 12.7 `test_ipc.py` 最后打印 ✅ 时报 `UnicodeEncodeError`（GBK 控制台）

**症状**：`test_ipc.py` 所有 `[CHECK] ... OK` 都通过，但末尾抛 `UnicodeEncodeError: 'gbk' codec can't encode character '✅'`，退出码非 0。

**根因**：中文 Windows 控制台默认 GBK 编码，无法编码 ✅（U+2705）。脚本最后一行 `print("\n全部通过 ✅")` 触发；**此时所有断言已通过、引擎也已正常关闭**，属误报。

**修复/规避**（任选其一）：
```powershell
python -X utf8 test_ipc.py          # 临时
$env:PYTHONUTF8=1; python test_ipc.py   # 当前会话
chcp 65001; python test_ipc.py      # 切控制台到 UTF-8
```

### 12.8 analyzer 状态栏一直「加载模型中…」或变红「已退出」

- **一直加载**：OpenCL 第一次会做 GPU 调优，可能几十秒到数分钟，并会把结果写入 `katago-runtime\KataGoData\opencltuning\`；之后启动会快很多。若反复超过数分钟仍未就绪，看 `analysis_logs\` 最新日志或 `katago_client` 的 `recent_stderr`。
- **已退出（红）**：引擎子进程崩溃。最常见是 DLL 缺失（见 12.1）或模型路径错（见 12.6）。GUI 会显示 `recent_stderr(8)` 的最后几行。

### 12.9 `python app.py` 报 `ModuleNotFoundError: No module named 'tkinter'`

重装 Python 时未勾选 tcl/tk，或用的是精简发行版。用官方 python.org 安装包重装（默认含 tkinter），或改用 `py app.py`。

### 12.10 求助渠道

- 围棋计算机 Discord 的 #help 频道：https://discord.gg/bqkZAz3
- GitHub Issues（附完整命令、报错、配置、网络、显卡、系统）：https://github.com/lightvector/KataGo/issues

---

## 13. 来源与校验

所有产物出处可追溯、且已校验：

| 产物 | 来源 | 校验 |
|---|---|---|
| 源码 [KataGo/](KataGo/) | `git clone https://github.com/lightvector/KataGo.git` | `git` 成功，79.5 MB |
| 引擎 `katago-opencl.exe` / `katago-eigenavx2.exe` | [GitHub Release v1.16.5](https://github.com/lightvector/KataGo/releases/tag/v1.16.5)（2026-05-25，非预发布） | 解压自官方 zip；`version` 输出正确 |
| 模型 `b18c384nbt` | https://katagotraining.org/ | HTTP 200，**字节级精确匹配 97,898,094** |
| 模型 `b28c512nbt` | https://katagotraining.org/ | HTTP 200，**字节级精确匹配 271,440,852** |
| 分析器 `analyzer/` | 本项目自研 | 核心无头测试 **137 项**通过（精细评价 + 长期画像 + 训练分析 + 棋风 + 错题本 + 终局点目 + 复盘 + 项目文件 + 棋谱库 + 真实 KataGo 通信等，见下） |

**端到端实测**：
- **引擎**：CPU 版加载默认模型，于 9×9 空棋盘生成棋步 `E5`；引擎日志含 `KataGo v1.16.5`、`Model version 14`、`Model name: kata1-b18c384nbt-s9996604416-d4316597426`。
- **分析器**（核心测试脚本覆盖纯逻辑、项目文件与真实 KataGo 通信）：
  - 坐标转换全部正确（`(0,0)=A19`、`(15,3)=Q16`、第 9 列=J 跳过 I 等）；
  - 棋盘规则全部正确（提子计数、纯自杀禁着、打劫回提禁着）；
  - MoveTree 导航/分支全部正确（`moves_list=[['B','D16'],['W','Q4']]`、撤回/快进/新分支）；
  - **真实 KataGo 通信闭环**：`query_version` 返回 `version=1.16.5, git_hash=ba938676...`；空盘 19×19（chinese、komi 7.5）首选 `Q16`、黑方胜率 0.374、目差 −0.94、PV=`Q16 D4 C3 D3 C4 D5`；落 Q16 后 `currentPlayer=W`（轮到对方），交叉印证坐标往返正确。
  - **终局点目**（v3.9）：面积法计分（黑/白活子+地+贴目）、死子移除重算、中立空区、ownership 死子建议、确认后 SGF 写 `RE[]`+`C[]` 摘要（保留原 RE）——`test_score_estimator.py`(10) + `test_sgf_variations.py` 点目 4 项 + `test_ui_smoke.py` 点目段全绿，并经 26-agent 对抗式审查（修复 7 项缺陷）。
  - **复盘三件套**（v3.10）：每手目损（黑/白走子方视角翻转）、失误榜 Top N、胜率/目差序列、批量分析进度——`test_review.py`(10) + `test_ui_smoke.py` 复盘/批量段全绿（手动构造 analysis dict，不启动引擎）。
  - **易用性**（v3.11）：自动启动 quiet 不弹窗、Home/End/PgUp/PgDn 跳转、Space 自动播放起停/末尾自停/落子自停、点目模式拦截快捷键——`test_ui_smoke.py` 易用性段全绿。

---

## 14. 附录 A：C++ 源码分层架构

引自 [KataGo/cpp/README.md](KataGo/cpp/README.md)，由低到高（均在 `cpp/` 下）：

| 层 | 目录 | 职责 |
|---|---|---|
| 外部库 | `external/` | 捆绑的第三方小库 |
| 基础 | `core/` | 标准库之上的底层工具（哈希、随机、字符串/文件系统） |
| 棋盘 | `game/` | 棋盘表示与规则：`board.*`（裸棋盘 + Benson 算法 + 征子）、`boardhistory.*`（着子历史、超级劫、虚手、终局、计分、贴目、让子）、`graphhash.*`（MCGS 用的历史敏感哈希） |
| 神经网络 | `neuralnet/` | GPU NN 实现与共享接口：`desc`(结构+权重)、`modelversion`、`nninputs`、`nninterface.h`、各后端 `{cuda,opencl,eigen,trt,metal,dummy}backend.cpp`、顶层线程安全批处理句柄 `nneval.*` |
| 搜索 | `search/` | 主引擎：`timecontrols`、`searchparams`(可调系数)、`search.*`(多线程 MCTS)、`searchresults`、`asyncbot`(线程安全思考层) |
| 数据 IO | `dataio/` | SGF 读写、自训练数据写出（`sgf`、`loadmodel`、`trainingwrite`）；`homedata.cpp` 定义默认搜索目录 |
| 开局库 | `book/` | 开局库生成工具 |
| 程序 | `program/` | 顶层辅助：`setup`(配置解析)、`playutils`(让子、归属、统计、benchmark)、`play`(对局与自对弈) |
| 分布式 | `distributed/` | 与 katagotraining.org 通信的客户端 |
| 测试 | `tests/` | 各类测试（含小型测试网） |
| 命令 | `command/` | 用户面子命令（`gtp`/`analysis`/`benchmark`/`contribute`/`selfplay`/`gatekeeper`/`match` 等） |
| 配置 | `configs/` | 各子命令默认/示例配置 |

> KataGo 用 **蒙特卡洛图搜索（MCGS）** 而非朴素树 MCTS：在 DAG 上共享转置节点，分别跟踪边访问计数与子节点访问，递归地、幂等地计算 Q；用 Zobrist 风格的 `graphhash` 配合围棋特定定理限定环长。详见 [GraphSearch.md](KataGo/docs/GraphSearch.md)。
>
> 对比：本项目 `analyzer/` 的 `board.py` 实现的是**朴素的简单规则**（提子/自杀/单步打劫），用于本地落子合法性判定；终局计分/复杂劫/让子等完整规则由引擎侧负责（analysis 查询带 `rules`/`komi`，引擎按其规则集判定并返回）。

---

## 15. 附录 B：训练历史与研究方法亮点

引自 [KataGo/TrainingHistory.md](KataGo/TrainingHistory.md) 与 [KataGo/KataGoMethods.md](KataGo/KataGoMethods.md)。

### 15.1 主要训练 run

- **g65**（2019-02，7 天，至多 35 张 V100）：早期 arXiv 论文对应 run，达到约 LZ130（勉强超人类）。
- **g104**（2019 年 5–6 月，至多 28 张 V100，19 天）：3.5 天即超越 g65；最终 20-block 网在等访问量下略强于 Facebook ELFv2。
- **g170**（2019-12 ～ 2020-06，约 5 个月，主要 44–46 张 V100）：12–14 天超越 g104；结束时比 LZ 最强 40-block 网强 **700+ Elo**；加入日本规则、强让子、精确比分估计。最终网约 1908–1919 Elo（@1200 visits）。
- **kata1**（2020 至今，公共分布式 run）：延续 g170 的巅峰，[katagotraining.org](https://katagotraining.org/)。本项目两网即来自此 run。

### 15.2 关键方法（AlphaZero 之外）

| 方法 | 引入版本 | 影响 |
|---|---|---|
| 多棋盘尺寸遮罩训练 | 早期 | 单网通吃多尺寸、可泛化到更大棋盘 |
| Fixed-Variance 初始化 + 单 BatchNorm | v1.12 (2023-03) | 替代 Fixup，训练快 1.6–1.8×、易多卡 |
| 整形 Dirichlet 噪声 + 根策略 softmax 温度 | g170 | 把噪声集中到「合理但被忽视」的点上 |
| **策略惊奇度加权** | g170 | 上调让 MCTS 策略目标「惊奇」先验的样本，g170 较大增益之一 |
| 子树价值偏差校正 | 2020 末 | 按模式分桶在线修正 NN 价值误差，**+30~60 Elo** |
| 动态方差缩放 cPUCT + 不确定性加权 playout | v1.9 (2021-06) | 合计较上一版 **~+75 Elo** |
| **嵌套瓶颈残差网** | v1.13 (2023-03) | 新 `b18c384` 与老 `b40c256` 同速、却接近老 `b60c320` 的单次评估强度——本项目 `b18`/`b28` 网即此架构 |
| 辅助软策略目标 | v1.12 | 预测 `policy^(1/T)`，大幅加速策略学习 |
| 乐观策略（Optimistic Policy） | v1.13 (2023-06) | 仅在测试期 MCTS 双方使用，**+40~90 Elo** |

> 前身项目 [GoNN](https://github.com/lightvector/GoNN)（在职业棋谱上监督学习）贡献了全局池化与归属辅助预测思想。

---

*文档完（v2，2026-06-21 更新：补全 analyzer 应用层章节、修正项目路径为 `d:\katago\`、新增 12.6–12.9 排错条目）。如有疑问或想扩展（如给 analyzer 加 SGF 导入导出、地盘热力图、或修复 test_ipc.py 的 GBK 编码问题），可随时提出。*
